# Risk Log — HypeAgentSocials design phase

*Seeded Wave 0 (2026-08-05). Append-only per wave; single writer per wave. Each row: risk, consequence if ignored, owning brief/section, status.*

---

## Research flags (scouted 2026-08-05, review-hardened; assigned briefs MUST address)

| ID | Risk | Consequence if ignored | Owner |
|----|------|------------------------|-------|
| F-1 | Reddit ToS prohibits commercial API use; OAuth approval manual 2–4 wks; public JSON endpoints same-ToS + CDN-blocked in practice | Architecture built on an illegal/unavailable source; realistic fallbacks = licensed aggregator or human-curated input | T5/T6 (facts), T15 (legal), OD-2 |
| F-2 | X free API tier is dead; read path ≠ publish path (Postiz user-OAuth publish is a separate decision) | Conflating reads with publishing blocks both; reseller ToS affects artifact retention | T5 (reads), T9 (publish), OD-1 |
| F-3 | Model churn is structural (e.g. Sora 2 API sunset ~Sept 2026); churn also invalidates prompt patterns and price tiers | Any single-model pin rots before build; deliverable = model registry concept with last-verified dates + re-verification cadence | T2, §5 |
| F-4 | Publish destinations need a config allowlist with ONE enforcement point + defense-in-depth (research-only channels never connected in Postiz) | Scattered per-source checks → one missed path auto-publishes to a research platform | T9, §7/§11 |
| F-5 | Native-audio video models → spoken fake claims are a new safety surface; Czech speech quality materially weaker | Claim gate that only reads text misses audio; possible en/cs pipeline fork | T1/T2/T4 (facts), T14 (enforcement: script-lock vs ASR-verify), §14 |
| F-6 | Higgsfield bundles UGC Builder + Marketing Studio + agent orchestrator; sparse API docs | Mis-classifying complement-vs-competitor skews provider architecture; paper-only eval must carry confidence tags | T2, OD-3 |
| F-7 | Czech is not a translation pass: cs slop lexicon ≠ translated en; video models mangle Czech diacritics on-screen; cs TTS quality varies; cs platform norms differ | Czech outputs read as obvious AI slop → brand damage in home market | T8 (empirical), T13 (rubric), T4 (rendering/TTS), all briefs state cs behavior |
| F-8 | EU AI Act Art. 50 synthetic-content transparency in force + separate platform-native AI labels (TikTok AIGC, YouTube altered-content, Meta AI info); re-encode/assembly typically strips C2PA provenance | Non-compliant published assets; provenance silently lost mid-pipeline — architecture input, not a legal footnote | T15 (law), T4 (C2PA survival), T9 (label fields), §14 |
| F-9 | Scraping pessimism default: headless detection near-total on major surfaces; LinkedIn no public read API; Google Trends no stable official API; Meta Ad Library API needs ID verification | Architecture assuming easy scraping collapses on contact; honest shape ≈ APIs/licensed data + operator-supplied inputs, Playwright only where genuinely open | T5/T6, §2 |

## Review-risk areas (from plan review; R1 verifies each in W4)

| ID | Risk area | What must be concretely true in the plan |
|----|-----------|------------------------------------------|
| RA-1 | Cron idempotency/dedupe | Concrete run identity, dedupe keys, overlap policy — not hand-waved |
| RA-2 | Brand-fit scoring | Inspectable sub-scores + hard skip threshold; anti-forced-placement |
| RA-3 | Anti-slop gate | Machine gate with bounded regenerate + escalate-to-review; never silently ship |
| RA-4 | Source-access honesty | X under D-08, Reddit per F-1, F-9 pessimism throughout — no fantasy access paths |
| RA-5 | Brand-truth conflicts | Precedence + confidence bands + degrade trigger defined |
| RA-6 | Budget caps & dry-run | Caps incl. mid-pack cap-hit behavior; dry-run boundary explicit |
| RA-7 | cs+en output-set integrity | D-02 first-class in every layer (F-7), not bolted on |
| RA-8 | Mode enforcement | ONE fail-closed choke point, capability matrix per mode |

## Operational risks (seeded W0)

| ID | Risk | Consequence | Owner |
|----|------|-------------|-------|
| OP-1 | D-02 doubles media spend against trial quotas (cs + en = 2× video generations per pack); confirmed W0.5: only **$50 Kie credits** exist | Kie trial credits exhausted mid-evaluation; unit economics must price the doubling explicitly against the $50 reality | T2 (economics), §5 |
| OP-2 | No Postiz account exists yet (W0.5) → draft-without-schedule kill-switch question cannot be verified empirically during design | Distribution architecture rests on paper claims; fallback ladder (internal draft → far-future schedule → local-only staging + manual paste) must be designed as first-class, and Postiz capability verification becomes an implementation-phase acceptance criterion | T9, §7 |

## Risks surfaced by Wave-1 research (appended 2026-08-06, T16 — "(W2 synthesis)")

| ID | Risk | Consequence if ignored | Mitigation established | Owner / brief |
|----|------|------------------------|------------------------|---------------|
| W2-01 (W2 synthesis) | **Curated-inbox ritual dependency.** The ICP-pain axis depends entirely on the operator running a weekly Reddit Pro + thread-curation session. A skipped week is invisible in the output | The pipeline silently loses its best pain source while packs continue to look complete; content drifts toward launch-hype only | Staleness flag per source; digest banner naming the axis as degraded; packs explicitly labelled "pain axis: operator-fed"; two consecutive misses escalate notification prominence | B1 §3.1, B2 §2.8 |
| W2-02 (W2 synthesis) | **Kie ephemerality × no idempotency × disputed billing.** Media deleted at 14 days, result URLs expire sooner, no idempotency key exists, and Kie's "failed tasks aren't charged" claim is contradicted by community reports | Paid work lost after the fact, or double-paid on retry, or charged with no artifact to show for it | Write-ahead spend ledger; resolve-by-query on restart; named `submitted-unknown` state with no auto-resubmit; expiry-ordered download queue drained first each run; checksum on re-host; balance-delta reconciliation with an unexplained-spend circuit breaker | A2 §2.6/2.8 × C3 — D-17 |
| W2-03 (W2 synthesis) | **Silent provider model substitution.** Kie's Veo route can switch to a backup model on content-review triggers; the substitute cannot use the 1080p endpoint and is forced to 16:9 | A 16:9 asset silently enters a 9:16 destination and fails at publish time; worse, the per-asset license snapshot names a model that did not render it, destroying the rights-defence record | Record requested-vs-delivered route, aspect and resolution; resolve the license snapshot *after* completion; surface any mismatch in the pack | A2 §2.7 × C7 §2.8 — D-20 |
| W2-04 (W2 synthesis) | **EU AI Act Art. 50 exposure.** Binding since 2026-08-02, no size exemption, up to €15M or 3% of turnover; metadata-only compliance provably fails because platforms strip C2PA on re-encode | Non-compliant published assets with a real penalty regime, and a false belief that provenance metadata is doing compliance work | Burned-in human-perceivable disclosure at render time as the load-bearing control; publish-gate precondition; C2PA signed after final encode and archived, never relied on; no end-to-end survival claims | C7 §2.4/2.4a, A4 §2.9 — D-19 |
| W2-05 (W2 synthesis) | **The publishing bridge cannot carry the label.** Postiz exposes no per-platform AI-disclosure fields, so labelling is a manual human action | A busy operator skips it; platforms auto-detect and demote, strike or remove; Meta reports undisclosed AI as a leading ad-rejection cause | Per-asset "AI label required" flag; pack checklist item; publish gate refuses "ready" without explicit acknowledgement; re-test whether the field appears at trial | C1 §7 |
| W2-06 (W2 synthesis) | **GDPR on research artifacts.** Public social content is personal data regardless of pseudonymity; no legitimate-interest assessment or published privacy notice exists yet; erasure and objection must be executable | Unlawful processing from day one; inability to honour an Art. 21 objection; monolithic raw dumps that cannot be selectively deleted | Extract-first storage, bounded retention, hashed or dropped author handles, targeted deletion by canonical key from day one; LIA and privacy notice named as prerequisite company artefacts | C7 §2.6, B2 §2.7 — OD-15 |
| W2-07 (W2 synthesis) | **Czech output suppressed by arithmetic.** No Czech-signal carrier exposes per-item engagement, so a four-factor multiplicative composite drives every cs candidate toward zero | D-02 fails silently — every policy document still says Czech is first-class while the scorer quietly emits nothing | cs composite drops the virality factor rather than proxying it; "under-evidenced ≠ poor fit" becomes the normal cs path; theme-readiness asserts a non-empty candidate set per language | B3 re-derived vs B1/B2 — D-22 |
| W2-08 (W2 synthesis) | **Czech asset-mix reputational risk.** Entertainment-styled short-form reads as cheap or unprofessional to Czech B2B decision-makers; no credible Czech B2B short-form precedent exists | Home-market brand damage in pursuit of a volume benchmark drawn from English-language data | cs recipe = carousel-to-reel, education-first, LinkedIn and long-form YouTube destinations; TikTok excluded for cs in v1; D-02a records the interpretation | B4 §2 × A1 — D-02a |
| W2-09 (W2 synthesis) | **Vendor-blog statistics leaking into hard thresholds.** Several headline numbers across A1, B4 and C4 come from marketing-vendor blogs, some labelled High confidence by their own briefs | Scoring thresholds, volume targets and review budgets calibrated on unverifiable marketing claims, then defended as evidence-based | Standing rule: no threshold may be set from a Low/Medium marketing-blog statistic; thresholds come from measured run data; confidence downgraded in the consolidated fact ledger | SYNTHESIS §6 standing caution |
| W2-10 (W2 synthesis) | **Judge over-strictness cost spiral.** A too-strict judge in an unattended run pays (1+cap) generation *and* (1+cap) judge calls per artifact for zero quality gain, then floods the review queue | Budget burned overnight; alert fatigue trains the operator to ignore flags; the gate becomes theatre | Per-pack (not per-asset) retry allowance; rolling flag-rate tracked as a judge-health signal distinct from individual flags; start lenient on a new language and tighten on real agreement data | C5 §4 |
| W2-11 (W2 synthesis) | **Notion plan-vs-fact contamination.** Roadmap, draft and aspiration pages sit beside fact pages and are fresh, well-written and internally consistent — confidence scoring cannot catch them | The system confidently asserts capabilities the product does not have; the highest-probability false "we do X" claim in this deployment | Structural control, not statistical: read only designated fact locations; any offer whose status is not explicitly `live` is unspinnable, enforced by REST property filters (MCP cannot do this) | C6 §3.4 × C1 §1 |
| W2-12 (W2 synthesis) | **Kie.ai's own legal terms are unread.** Every kie.ai legal path was blocked to automated retrieval and no archive snapshot exists | The router's actual grants, disclaimers and liability position are unknown while the pipeline depends on it | Treat router terms as granting nothing (OpenRouter/Replicate structural analogy); per-asset upstream license snapshot is the real control; manual browser pull before build sign-off | C7 §2.8, ledger row 35 |
| W2-13 (W2 synthesis) | **Review UX assumes affordances the runtime does not fund.** C4 describes live buttons and a browser workflow; D-07 specifies a folder, C3 a console process with no server | An architecture written against C4 literally would require a web app nobody decided to build | Digest is a static file in the run folder; approval by decision file or console command referencing the run id; C4's decision *content* adopted, interaction *mechanism* replaced | C4 vs D-07/C3 — SYNTHESIS §3.11 |
| W2-14 (W2 synthesis) | **Trial-budget exhaustion.** $50 buys ≈13 two-language standard packs; one mis-configured run or an unbounded retry loop can consume a large share | The evaluation ends before the pipeline is validated, with no evidence to show for the spend | Pre-submission cap enforcement (per asset / run / day / month); hero tier never auto-selected; documented trial plan ($8 bake-off / $35 packs / $7 reserve); separate cron API key with a bounded top-up | A2 §2.11, OP-1 |
| W2-15 (W2 synthesis) | **Meta Ad Library token expiry breaks cron silently.** 60-day tokens plus a personal government-ID verification step gating the whole source | The ad-creative axis dies quietly mid-quarter; the operator discovers it weeks later | Token-expiry alarm with fail-closed behaviour; renewal as a named runbook item; ID verification scheduled as week-1 onboarding, not a technical detail | B1 §3.3, B2 §2.5 |
| W2-16 (W2 synthesis) | **Model and vendor churn is structural — now evidenced twice.** Sora 2's API dies 2026-09-24; Trendpop died mid-2026 and redirects to its acquirer; Kie cut prices twice in 2026 | Any pinned model or vendor name rots before build; a dead vendor takes an entire signal axis with it | Model registry and vendor roster both carry last-verified / recheck-by dates; a lapsed recheck drops an entry to degraded and it stops being selected for spend; every licensed source has a designed degraded state and a swap path | A2 §2.1.6 (F-3) extended to vendors by B5 §3.5 |
| W2-17 (W2 synthesis) | **Trend-vendor adoption rests on an unresolved vendor-internal contradiction.** Virlo's MCP guide says any paid plan includes full API access; its pricing page lists API access as Enterprise-only | Paying $49/month for a capability that turns out to be Enterprise-gated, with the short-form trend axis silently absent | Adoption is trial-gated on exactly this question; Shortimize named as the same-category fallback; monthly Creative Center ritual is the designed degraded state if both fail | B5 §3.2 — OD-16 |
| W2-18 (W2 synthesis) | **Derived-analytics vendors have undisclosed, unlicensable upstream collection.** No platform licence programme exists for TikTok/IG/YT trend data, so every vendor in this category collects by means it does not describe | A compliance posture inherited by assumption rather than by analysis, in a project whose whole source strategy is built on legitimate access | Working assumption recorded (derived analytics ≠ raw passthrough; same posture as Exploding Topics/BuzzSumo); raw-passthrough class explicitly excluded; carried to C7 as OD-17 rather than left implicit | B5 §3.2, B2 §2.9 |
| W2-19 (W2 synthesis) | **Prompt injection through collected content.** All collected text is adversarial and will sit inside ranking, spin and copy prompts | A crafted post title redirects a downstream model call; the failure is silent and looks like a bad topic choice | Collected text carried as quoted data with provenance tags, never as instructions; the instruction layer stays structurally separate and privileged; injection-style phrasing is itself a veto signal | B2 §2.6, B3 §6.4 |
| W2-20 (W2 synthesis) | **Cross-platform runtime traps that only appear under a scheduler.** SYSTEM-account profile isolation, per-user credential stores, Czech console code pages, cron's minimal environment, DST double-fires | Works interactively, fails at 03:00 — the hardest class of bug to diagnose because there is no console to watch | Run as the operator's own account; force UTF-8 at every entry point; absolute paths from config; file logging as the only observability; UTC-internal timestamps with a pinned run-date; explicit exit-code propagation from any wrapper | C2 §2.6/2.7, C3 §2.2 |
