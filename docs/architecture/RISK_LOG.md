# Risk Log — HypeAgentSocials design phase

*Seeded Wave 0 (2026-08-05). Append-only per wave; single writer per wave. Each row: risk, consequence if ignored, owning brief/section, status.*

---

## Research flags (scouted 2026-08-05, review-hardened; assigned briefs MUST address)

| ID | Risk | Consequence if ignored | Owner |
|----|------|------------------------|-------|
| F-1 | Reddit ToS prohibits commercial API use; OAuth approval manual 2–4 wks; public JSON endpoints same-ToS + CDN-blocked in practice | Architecture built on an illegal/unavailable source; realistic fallbacks = licensed aggregator or human-curated input | T5/T6 (facts), T15 (legal), OD-2 |
| F-2 | X free API tier is dead; read path ≠ publish path (Postiz user-OAuth publish is a separate decision) | Conflating reads with publishing blocks both; reseller ToS affects artifact retention | T5 (reads), T9 (publish), OD-1 |
| F-3 | Model churn is structural (e.g. Sora 2 API sunset ~Sept 2026); churn also invalidates prompt patterns and price tiers | Any single-model pin rots before build; deliverable = model registry concept with last-verified dates + re-verification cadence | T2, §5 |
| F-4 | Publish destinations need a config allowlist with ONE enforcement point + defense-in-depth (research-only channels never connected in Postiz) | Scattered per-source checks → one missed path auto-publishes to a research platform | T9, §7/§11 |
| F-5 | Native-audio video models → spoken fake claims are a new safety surface; Czech speech quality materially weaker | Claim gate that only reads text misses audio; possible en/cs pipeline fork | T1/T2/T4 (facts), T14 (enforcement: script-lock vs ASR-verify), §14 |
| F-6 | Higgsfield bundles UGC Builder + Marketing Studio + agent orchestrator; sparse API docs | Mis-classifying complement-vs-competitor skews provider architecture; paper-only eval must carry confidence tags | T2, OD-3 |
| F-7 | Czech is not a translation pass: cs slop lexicon ≠ translated en; video models mangle Czech diacritics on-screen; cs TTS quality varies; cs platform norms differ | Czech outputs read as obvious AI slop → brand damage in home market | T8 (empirical), T13 (rubric), T4 (rendering/TTS), all briefs state cs behavior |
| F-8 | EU AI Act Art. 50 synthetic-content transparency in force + separate platform-native AI labels (TikTok AIGC, YouTube altered-content, Meta AI info); re-encode/assembly typically strips C2PA provenance | Non-compliant published assets; provenance silently lost mid-pipeline — architecture input, not a legal footnote | T15 (law), T4 (C2PA survival), T9 (label fields), §14 |
| F-9 | Scraping pessimism default: headless detection near-total on major surfaces; LinkedIn no public read API; Google Trends no stable official API; Meta Ad Library API needs ID verification | Architecture assuming easy scraping collapses on contact; honest shape ≈ APIs/licensed data + operator-supplied inputs, Playwright only where genuinely open | T5/T6, §2 |

## Review-risk areas (from plan review; R1 verifies each in W4)

| ID | Risk area | What must be concretely true in the plan |
|----|-----------|------------------------------------------|
| RA-1 | Cron idempotency/dedupe | Concrete run identity, dedupe keys, overlap policy — not hand-waved |
| RA-2 | Brand-fit scoring | Inspectable sub-scores + hard skip threshold; anti-forced-placement |
| RA-3 | Anti-slop gate | Machine gate with bounded regenerate + escalate-to-review; never silently ship |
| RA-4 | Source-access honesty | X under D-08, Reddit per F-1, F-9 pessimism throughout — no fantasy access paths |
| RA-5 | Brand-truth conflicts | Precedence + confidence bands + degrade trigger defined |
| RA-6 | Budget caps & dry-run | Caps incl. mid-pack cap-hit behavior; dry-run boundary explicit |
| RA-7 | cs+en output-set integrity | D-02 first-class in every layer (F-7), not bolted on |
| RA-8 | Mode enforcement | ONE fail-closed choke point, capability matrix per mode |

## Operational risks (seeded W0)

| ID | Risk | Consequence | Owner |
|----|------|-------------|-------|
| OP-1 | D-02 doubles media spend against trial quotas (cs + en = 2× video generations per pack); confirmed W0.5: only **$50 Kie credits** exist | Kie trial credits exhausted mid-evaluation; unit economics must price the doubling explicitly against the $50 reality | T2 (economics), §5 |
| OP-2 | No Postiz account exists yet (W0.5) → draft-without-schedule kill-switch question cannot be verified empirically during design | Distribution architecture rests on paper claims; fallback ladder (internal draft → far-future schedule → local-only staging + manual paste) must be designed as first-class, and Postiz capability verification becomes an implementation-phase acceptance criterion | T9, §7 |

## Risks surfaced by Wave-1 research (appended 2026-08-06, T16 — "(W2 synthesis)")

| ID | Risk | Consequence if ignored | Mitigation established | Owner / brief |
|----|------|------------------------|------------------------|---------------|
| W2-01 (W2 synthesis) | **Curated-inbox ritual dependency.** The ICP-pain axis depends entirely on the operator running a weekly Reddit Pro + thread-curation session. A skipped week is invisible in the output | The pipeline silently loses its best pain source while packs continue to look complete; content drifts toward launch-hype only | Staleness flag per source; digest banner naming the axis as degraded; packs explicitly labelled "pain axis: operator-fed"; two consecutive misses escalate notification prominence | B1 §3.1, B2 §2.8 |
| W2-02 (W2 synthesis) | **Kie ephemerality × no idempotency × disputed billing.** Media deleted at 14 days, result URLs expire sooner, no idempotency key exists, and Kie's "failed tasks aren't charged" claim is contradicted by community reports | Paid work lost after the fact, or double-paid on retry, or charged with no artifact to show for it | Write-ahead spend ledger; resolve-by-query on restart; named `submitted-unknown` state with no auto-resubmit; expiry-ordered download queue drained first each run; checksum on re-host; balance-delta reconciliation with an unexplained-spend circuit breaker | A2 §2.6/2.8 × C3 — D-17 |
| W2-03 (W2 synthesis) | **Silent provider model substitution.** Kie's Veo route can switch to a backup model on content-review triggers; the substitute cannot use the 1080p endpoint and is forced to 16:9 | A 16:9 asset silently enters a 9:16 destination and fails at publish time; worse, the per-asset license snapshot names a model that did not render it, destroying the rights-defence record | Record requested-vs-delivered route, aspect and resolution; resolve the license snapshot *after* completion; surface any mismatch in the pack | A2 §2.7 × C7 §2.8 — D-20 |
| W2-04 (W2 synthesis) | **EU AI Act Art. 50 exposure.** Binding since 2026-08-02, no size exemption, up to €15M or 3% of turnover; metadata-only compliance provably fails because platforms strip C2PA on re-encode | Non-compliant published assets with a real penalty regime, and a false belief that provenance metadata is doing compliance work | Burned-in human-perceivable disclosure at render time as the load-bearing control; publish-gate precondition; C2PA signed after final encode and archived, never relied on; no end-to-end survival claims | C7 §2.4/2.4a, A4 §2.9 — D-19 |
| W2-05 (W2 synthesis) | **The publishing bridge cannot carry the label.** Postiz exposes no per-platform AI-disclosure fields, so labelling is a manual human action | A busy operator skips it; platforms auto-detect and demote, strike or remove; Meta reports undisclosed AI as a leading ad-rejection cause | Per-asset "AI label required" flag; pack checklist item; publish gate refuses "ready" without explicit acknowledgement; re-test whether the field appears at trial | C1 §7 |
| W2-06 (W2 synthesis) | **GDPR on research artifacts.** Public social content is personal data regardless of pseudonymity; no legitimate-interest assessment or published privacy notice exists yet; erasure and objection must be executable | Unlawful processing from day one; inability to honour an Art. 21 objection; monolithic raw dumps that cannot be selectively deleted | Extract-first storage, bounded retention, hashed or dropped author handles, targeted deletion by canonical key from day one; LIA and privacy notice named as prerequisite company artefacts | C7 §2.6, B2 §2.7 — OD-15 |
| W2-07 (W2 synthesis) | **Czech output suppressed by arithmetic.** No Czech-signal carrier exposes per-item engagement, so a four-factor multiplicative composite drives every cs candidate toward zero | D-02 fails silently — every policy document still says Czech is first-class while the scorer quietly emits nothing | cs composite drops the virality factor rather than proxying it; "under-evidenced ≠ poor fit" becomes the normal cs path; theme-readiness asserts a non-empty candidate set per language | B3 re-derived vs B1/B2 — D-22 |
| W2-08 (W2 synthesis) | **Czech asset-mix reputational risk.** Entertainment-styled short-form reads as cheap or unprofessional to Czech B2B decision-makers; no credible Czech B2B short-form precedent exists | Home-market brand damage in pursuit of a volume benchmark drawn from English-language data | cs recipe = carousel-to-reel, education-first, LinkedIn and long-form YouTube destinations; TikTok excluded for cs in v1; D-02a records the interpretation | B4 §2 × A1 — D-02a |
| W2-09 (W2 synthesis) | **Vendor-blog statistics leaking into hard thresholds.** Several headline numbers across A1, B4 and C4 come from marketing-vendor blogs, some labelled High confidence by their own briefs | Scoring thresholds, volume targets and review budgets calibrated on unverifiable marketing claims, then defended as evidence-based | Standing rule: no threshold may be set from a Low/Medium marketing-blog statistic; thresholds come from measured run data; confidence downgraded in the consolidated fact ledger | SYNTHESIS §6 standing caution |
| W2-10 (W2 synthesis) | **Judge over-strictness cost spiral.** A too-strict judge in an unattended run pays (1+cap) generation *and* (1+cap) judge calls per artifact for zero quality gain, then floods the review queue | Budget burned overnight; alert fatigue trains the operator to ignore flags; the gate becomes theatre | Per-pack (not per-asset) retry allowance; rolling flag-rate tracked as a judge-health signal distinct from individual flags; start lenient on a new language and tighten on real agreement data | C5 §4 |
| W2-11 (W2 synthesis) | **Notion plan-vs-fact contamination.** Roadmap, draft and aspiration pages sit beside fact pages and are fresh, well-written and internally consistent — confidence scoring cannot catch them | The system confidently asserts capabilities the product does not have; the highest-probability false "we do X" claim in this deployment | Structural control, not statistical: read only designated fact locations; any offer whose status is not explicitly `live` is unspinnable, enforced by REST property filters (MCP cannot do this) | C6 §3.4 × C1 §1 |
| W2-12 (W2 synthesis) | **Kie.ai's own legal terms are unread.** Every kie.ai legal path was blocked to automated retrieval and no archive snapshot exists | The router's actual grants, disclaimers and liability position are unknown while the pipeline depends on it | Treat router terms as granting nothing (OpenRouter/Replicate structural analogy); per-asset upstream license snapshot is the real control; manual browser pull before build sign-off | C7 §2.8, ledger row 35 |
| W2-13 (W2 synthesis) | **Review UX assumes affordances the runtime does not fund.** C4 describes live buttons and a browser workflow; D-07 specifies a folder, C3 a console process with no server | An architecture written against C4 literally would require a web app nobody decided to build | Digest is a static file in the run folder; approval by decision file or console command referencing the run id; C4's decision *content* adopted, interaction *mechanism* replaced | C4 vs D-07/C3 — SYNTHESIS §3.11 |
| W2-14 (W2 synthesis) | **Trial-budget exhaustion.** $50 buys ≈13 two-language standard packs; one mis-configured run or an unbounded retry loop can consume a large share | The evaluation ends before the pipeline is validated, with no evidence to show for the spend | Pre-submission cap enforcement (per asset / run / day / month); hero tier never auto-selected; documented trial plan ($8 bake-off / $35 packs / $7 reserve); separate cron API key with a bounded top-up | A2 §2.11, OP-1 |
| W2-15 (W2 synthesis) | **Meta Ad Library token expiry breaks cron silently.** 60-day tokens plus a personal government-ID verification step gating the whole source | The ad-creative axis dies quietly mid-quarter; the operator discovers it weeks later | Token-expiry alarm with fail-closed behaviour; renewal as a named runbook item; ID verification scheduled as week-1 onboarding, not a technical detail | B1 §3.3, B2 §2.5 |
| W2-16 (W2 synthesis) | **Model and vendor churn is structural — now evidenced twice.** Sora 2's API dies 2026-09-24; Trendpop died mid-2026 and redirects to its acquirer; Kie cut prices twice in 2026 | Any pinned model or vendor name rots before build; a dead vendor takes an entire signal axis with it | Model registry and vendor roster both carry last-verified / recheck-by dates; a lapsed recheck drops an entry to degraded and it stops being selected for spend; every licensed source has a designed degraded state and a swap path | A2 §2.1.6 (F-3) extended to vendors by B5 §3.5 |
| W2-17 (W2 synthesis) | **Trend-vendor adoption rests on an unresolved vendor-internal contradiction.** Virlo's MCP guide says any paid plan includes full API access; its pricing page lists API access as Enterprise-only | Paying $49/month for a capability that turns out to be Enterprise-gated, with the short-form trend axis silently absent | Adoption is trial-gated on exactly this question; Shortimize named as the same-category fallback; monthly Creative Center ritual is the designed degraded state if both fail | B5 §3.2 — OD-16 |
| W2-18 (W2 synthesis) | **Derived-analytics vendors have undisclosed, unlicensable upstream collection.** No platform licence programme exists for TikTok/IG/YT trend data, so every vendor in this category collects by means it does not describe | A compliance posture inherited by assumption rather than by analysis, in a project whose whole source strategy is built on legitimate access | Working assumption recorded (derived analytics ≠ raw passthrough; same posture as Exploding Topics/BuzzSumo); raw-passthrough class explicitly excluded; carried to C7 as OD-17 rather than left implicit | B5 §3.2, B2 §2.9 |
| W2-19 (W2 synthesis) | **Prompt injection through collected content.** All collected text is adversarial and will sit inside ranking, spin and copy prompts | A crafted post title redirects a downstream model call; the failure is silent and looks like a bad topic choice | Collected text carried as quoted data with provenance tags, never as instructions; the instruction layer stays structurally separate and privileged; injection-style phrasing is itself a veto signal | B2 §2.6, B3 §6.4 |
| W2-20 (W2 synthesis) | **Cross-platform runtime traps that only appear under a scheduler.** SYSTEM-account profile isolation, per-user credential stores, Czech console code pages, cron's minimal environment, DST double-fires | Works interactively, fails at 03:00 — the hardest class of bug to diagnose because there is no console to watch | Run as the operator's own account; force UTF-8 at every entry point; absolute paths from config; file logging as the only observability; UTC-internal timestamps with a pinned run-date; explicit exit-code propagation from any wrapper | C2 §2.6/2.7, C3 §2.2 |

## Amendments and additions at Wave 3b assembly (appended 2026-08-06, T19 — "(W3b assembler)")

*Append-only. The original W2-08 row above is left untouched so the history stays legible; the row below supersedes its **mitigation** only. The risk itself is unchanged and, if anything, larger.*

| ID | Risk | Consequence if ignored | Mitigation established | Owner / section |
|----|------|------------------------|------------------------|-----------------|
| **W2-08a (W3b assembler) — SUPERSEDES the mitigation recorded in W2-08** | **Czech asset-mix reputational risk, under the identical-mix rule.** W2.5-4 overruled the synthesis: Czech gets TikTok, Instagram Reels and YouTube Shorts in v1. W2-08's recorded mitigation ("cs recipe = carousel-to-reel, education-first, LinkedIn and long-form YouTube destinations; **TikTok excluded for cs in v1**; D-02a records the interpretation") is therefore **void** — D-02a is superseded by D-27 and the per-language channel table in `SYNTHESIS.md` §3.1 no longer describes the design | Home-market brand damage in the operator's own market, now without the channel reduction that was the original mitigation — and, worse, a plan that still *reads* as if the mitigation were in force | **Six design commitments replace the mix reduction (D-28):** (1) recipe not translation — carousel-to-reel with our own typography as the cs short-form default, no generative video model in the loop, diacritics correct by construction; (2) framing not mimicry — education-first, problem-to-solution rhythm, never an English hook rhythm in Czech words; (3) register discipline — vykání by default in every public post and first-contact CTA, tykání only on an explicit peer-community declaration; (4) understatement as a quality bar — the Czech judge weights the human-voice dimension higher than the English judge; (5) a destination-aware production floor enforced as a **hard gate** — every English assembly QA gate plus prosody acceptance, verified Czech glyph coverage, and no English audio anywhere (A1's English-audio-with-Czech-subtitles fallback stays rejected); (6) a **measurable revisit trigger** read from reason-coded rejections in the review-decision store, whose threshold is OD-22 | `ARCHITECTURE_PLAN.md` §3.1, §4.5, §12.2, §15.4; D-27, D-28 |
| **W3-01 (W3b assembler)** | **Theme-configuration surface breadth.** The resolved knob surface is roughly ninety settings across three config blocks plus the allowlist, the exemplar corpus and the shared language overlay. A misconfigured theme is now the single most likely cause of a bad-but-plausible pack — more likely than any provider or source failure, because a misconfiguration produces confident, complete-looking output | A theme that is wrong rather than broken: the wrong CTA class on the wrong domain, a language with an empty candidate set, a destination with no format profile, or a cadence pair that re-ranks a stale collection window every night | Everything that can spend, publish or relax a threshold **defaults to the safe value**, so a theme copied from another is safe before it is useful; both cadence knobs default off; mode defaults to test with an allowlist empty by construction; dry-run defaults on in test. **Theme-readiness validation is a gate on scheduling, not advice** — a theme failing readiness may be run interactively in test mode and may never be scheduled | `ARCHITECTURE_PLAN.md` §10.1, §10.6, §13.2 |
| **W3-02 (W3b assembler)** | **Per-run caps unresolved at build start.** W2.5-7 settled cadence (config knob, default off) but not volume; OD-8's second half — topics per run and media-bearing masters per language per run — is still open | Phase 4 begins against a small trial balance with an unagreed volume assumption baked into a default, and the trial ends before the pipeline is validated (the W2-14 failure, arriving through configuration rather than through a bug) | Caps are a Stage-5 answer with a recommendation already on the table (about five topics per run; one to two **masters** per language per run, per D-29); Phase 4's acceptance criterion requires eight to ten real two-language packs **inside the documented trial envelope with the reserve untouched**, which fails loudly if the caps were set too high | `ARCHITECTURE_PLAN.md` §16 OD-8, §17 Phase 4 |
| **W3-03 (W3b assembler)** | **Assets produced for a destination that is never published to.** X assets are produced at no marginal generation cost while X is config-gated default-off as a publish destination (D-30) | Review-time cost and operator irritation: unused assets in every pack, which is exactly the kind of friction that trains an operator to skim the digest instead of reading it | The choice is a single knob (OD-21) and the plan states both options and their costs; X never appears in any publish allowlist and is never a connected channel regardless of which is chosen | `ARCHITECTURE_PLAN.md` §3.2, §7.5, §16 OD-21 |

## Risks surfaced by the Wave-4 reviews and closed at Wave-5 (appended 2026-08-06, T25 — "(W5 fixer)")

*Append-only. Each row below was found by an independent Wave-4 reviewer, accepted at conductor triage, and mitigated in `ARCHITECTURE_PLAN.md`; the mapping from finding to edit is that document's Appendix B. Row **W3-01a** supersedes the sizing in W3-01's mitigation only — the risk and the rest of its mitigation stand.*

| ID | Risk | Consequence if ignored | Mitigation established | Owner / section |
|----|------|------------------------|------------------------|-----------------|
| **W4-01 (W5 fixer)** | **The text wallet was unbounded while the plan asserted it was capped.** ~130 text artifacts per run, each passing five model-mediated evaluations on top of drafting: 0.7–2.25M tokens per run and 9–27M per month at the recommended cadence — the same order of magnitude as the media forecast, with no provider role, no registry entry, no cap, no forecast line and no unit-economics row. §11.1 pointed LLM spend at a §8.11 cap that contained only media knobs | The orchestration argument that justifies the whole design — cost is computable before the run starts — is true of one wallet and false of the other; the operator's first surprise bill does not come from the media router; and the regeneration loops the plan itself names as the dominant unattended cost risk draw on a budget that does not exist | **Text-model provider role** in §5.1; registry extended to four route classes; **§5.4a LLM budget block** — per-run/day/month text budgets, per-stage call ceilings, per-pack judge allowance, per-node per-call token ceilings — enforced pre-call by the same cost gate at the same boundary as media, reusing the mid-pack cap-hit outcome class on exhaustion; **thirteen-node inventory** (§1.5) fixing what each node may consume; two-line forecast in the digest; text row in the economics table and in Appendix A.6 | Plan §1.5, §5.1, §5.2, §5.4, §5.4a, §8.11, §10.4, §11.1, §12.1 · R-36 · D-39 (T-6) |
| **W4-02 (W5 fixer)** | **Personality rights had no regime at all.** The plan carried a provider *content policy* (no-people / adults-only / region-restricted) and treated it as covering likeness — while keeping `talking human` as a live motion class and `voice identity` as a free-form knob. It would have stopped a generated public figure and not touched an employee "founder avatar", a licensed stock actor, or a cloned voice | Civil personality-rights exposure under Czech law (injunction, removal, monetary satisfaction), an employment dispute where the person is an employee, cumulative AI Act deep-fake obligations, and platform enforcement that names digital face replacement explicitly — arrived at not by a wrong decision but by silence | **A v1 ban (D-36):** no synthetic or synthetically altered human presenter and no cloned voice; `talking human` disabled; no identifiable-person reference inputs; `voice-basis` added to the registry with `cloned-real-person` a forbidden rights class; checked at keyframe acceptance so a violation costs four cents. The consent regime (person-basis flag, scope-limited consent record, hard publish-gate block on a missing reference) is designed and named as a **prerequisite** if the ban is ever lifted, with the employee agreement as OD-27 | Plan §5.3, §4.8, §4.10, §16 OD-27 · R-38 · D-36 (T-1) |
| **W4-03 (W5 fixer)** | **The load-bearing Article 50 control was configurable, and its default omitted audio.** The AI-content class was a per-theme knob defaulting to "realistic-synthetic for generated video and imagery" — so a theme could relax it, and the default silently exempted **CS-B, the Czech default recipe**, whose only generated component is a TTS voice | The entire Czech output set — the largest volume of assets in the pipeline — could legitimately class as non-synthetic and ship with no burned-in disclosure, against an article that names synthetic audio as its own modality, with fines up to €15M or 3% of turnover and no size exemption | AI-content class becomes an **engine-level derived field** computed from the generation record, covering synthetic speech and generated music; **configuration may only tighten, never relax** (the hard-excludes monotonic rule); Phase-3 acceptance test for a TTS-only asset carrying the disclosure. Separately, an **engine-level disclosure floor** (visible from the first second, safe box, minimum type-height ratio and contrast, asset language, audible equivalent) that themes may only tighten, measured in Phase 3 like the loudness gate | Plan §3.3, §4.4, §4.4a, §10.4, §17 Phase 3 · D-37 (T-2) |
| **W4-04 (W5 fixer)** | **The single publish-enforcement point had a structural bypass.** The publish gate triggered on a call to the publishing bridge; rung 3 — local-only staging for manual paste — makes no such call by definition, and is simultaneously the guaranteed floor, the path for every unconnected destination, **and the path for content the gate itself blocks** | The separately-recorded AI-label acknowledgement was skipped on the busiest failure path, precisely where a busy operator publishes by hand into platforms with their own labelling mechanics — and F-4/D-23's "one fail-closed enforcement point" was untrue in the scenario that matters most. (The burned-in disclosure was never at risk: it is a publish-ready precondition at assembly) | Label acknowledgement **moved to packaging**, travelling with the artefact as an unticked per-platform checklist inside the rung-3 file; §7.4's trigger restated as **"before any distribution preparation"**; Phase-6 acceptance test for a gate-blocked asset arriving in rung 3 with the checklist visible | Plan §7.2, §7.4, §17 Phase 6, Appendix A.10 · D-44 |
| **W4-05 (W5 fixer)** | **A publish-gate precondition rested on a field the evidence never showed is observable.** D-20 made delivered route identity mandatory at the gate; the source finding documents the substitution as *silent*, detectable only by side effects (aspect forced to 16:9, high-resolution endpoint unavailable). No delivered-model-identity field in the provider's response is evidenced anywhere | Either a permanent publish deadlock on every substituted asset — a rights-defence control turned into an outage — or, worse, an implementer quietly filling the field with the requested route and producing a licence snapshot naming a model that did not render the asset, which is the exact failure D-20 exists to prevent | Field downgraded to **best-effort with a three-state inference rule** (`identity-reported` / `substituted — identity unknown` / `assumed-as-requested`, the last flagged as an assumption); **publish-gate disposition for an unresolvable rights class is plan-only with the reason attached**, not an undisposed block; registry gains an **upstream-provider terms URL and read date** and defaults the rights class to the most restrictive value when upstream terms are unverifiable; "does the response name the rendering model" added to the Phase-0 router checklist | Plan §5.6, §5.2, §7.4, §12.2, §17 Phase 0/3 · R-08, R-36 · D-42 |
| **W4-06 (W5 fixer)** | **A 30-day verbatim-retention window nullified by permanent pack retention.** The provenance snapshot kept the quoted excerpt and the canonical permalink permanently with the run pack, and §12.2 kept per-signal source links forever — the same verbatim text the artifact store deleted at 30 days, in a different file. Targeted deletion had no stated path into an archived pack | Unlawful indefinite retention of personal data three rows below a policy saying otherwise; Article 17 and Article 21 requests unexecutable against every pack ever written; and a justification ("links and excerpts, not payloads") that is the wrong legal test and would have inverted in an audit | Provenance snapshot and per-signal source records **split** into a permanent de-identified part and a **30-day verbatim/permalink part** replaced in place by a placeholder; **run-pack → canonical-key index** added to the ledger set so deletion reaches archived packs; Phase-1 acceptance extended to deletion inside a packaged pack; keyed deterministic hashing of author handles with the key held separately; handles and permalinks redacted from prompt payloads; the wrong justification deleted | Plan §2.6, §2.6a, §8.6, §12.2, §17 Phase 1 · R-39 · D-43 |
| **W4-07 (W5 fixer)** | **Two designed safety mechanisms colliding to produce the outcome both prevent.** The low-disk check ran at two fixed points; phase 0's expiry-ordered drain must run to completion before any new submission and is backlog-dependent. Disk filling mid-drain triggered the designed hard-failure — abort before further writes — partway through | Already-generated, **already-paid-for** media left undownloaded against a 14-day provider deletion window with no guaranteed next attempt, which is precisely the "a crash or hard stop must not discard paid work" prime directive the whole checkpoint design exists to serve. Not a bug in either mechanism; a seam between two correct ones | Low-disk becomes a **running guard evaluated before each individual download**; on a mid-drain event the drain **narrows rather than aborts** — new submissions stop first, the drain continues in strict expiry order while space allows, ledger and pack headroom is reserved ahead of media writes; a genuine stop names the undownloaded jobs and their deletion deadlines; the pre-drain check estimates the whole backlog and refuses a drain it cannot finish | Plan §8.10, §8.13 · R-40 · D-48 |
| **W4-08 (W5 fixer)** | **A healthy outcome with no owner.** *completed-with-pending-media* was designed only as far as download and re-host: assembly consumes several media-job rows and had no ledger row, packaging's idempotency key broke the moment a completion changed the included-asset set, and the adopting run's mode and budget context were undefined. The plan's own worked example shipped a mastered, disclosure-bearing, reviewed master whose clip was still rendering | Either a prior run's pack mutated in place (destroying the idempotency property the plan asserts) or a second pack orphaning the review-decision rows keyed to the first run id and splitting one topic's review across two folders — plus a class of asset that can never complete, discovered only when a video is missing | **Assembly ledger** with blocked-on-inputs → assembling → assembled → failed, unblocked by its last input reaching `done`; assembly declared **non-spend-bearing** so any run may complete a blocked master; **pack amendment** with revision numbering, an amendment record against the originating run id, and cross-references in both digests; a pending master packaged as plan-only with its completed clips and a deletion countdown | Plan §4.7a, §8.6, §8.13, Appendix A · R-41 · D-48 |
| **W4-09 (W5 fixer)** | **Gate unavailability defaulted open.** The correct behaviour — *if the judgment step cannot run at all, fail closed; it never defaults open* — was specified once, for the brand-fit judgment, and for none of the six other gates. §8.10's blanket rule marks the artifact incomplete and says nothing about whether it may still be packed | An unattended run against a degraded judge endpoint produces a pack of never-semantically-gated assets sitting indistinguishably beside properly gated ones, and §14.2's promise that nothing is ever silently shipped is untrue of exactly the case nobody tests. Under the unattended fail-closed mandate, a gate that cannot run and therefore does not block *is* a policy violation | **A fifth fail-closed trigger** (§11.3): any gate or node that cannot execute fails its artifact closed to that gate's own named degraded outcome, with the gate named in the pack — enumerated for the voice judge, the claim semantic pass (deterministic verdicts only, all claim-shaped candidates blocked), the spin gate, the keyframe rubric, the platform gate, the asset QA rubric and node N-2. Phase-2 acceptance fires it for two gates | Plan §11.3, §14.2, §4.2a, §17 Phase 2 · R-37 · D-45 |
| **W4-10 (W5 fixer)** | **The one gate in the document with no ending.** The asset QA rubric explicitly does not use the refusal ladder, so the only route back from a QA-flagged defect is a human rejection — and the human-rejection immediate loop had no cap, no downgrade rule and no terminal state, unlike spin, voice and claim which each name a bounded ladder | A repeatedly-failing video regenerated an unbounded number of times, one rejection at a time, across sessions and runs, bounded only by the blunt dollar caps W2-10 already showed are insufficient for this exact failure shape. A perfectionist or distracted operator is the trigger, and nothing in the system says stop | **QA-rejection cap** (default two per asset slot per pack) counted in the media-job ledger so it survives across sessions, with a terminal state mirroring the voice gate's — ship the last version labelled "did not clear QA after N attempts" with every attempt attached. Four regeneration counters given distinct names and one **combined per-artifact repair ceiling** above them, with check order and exhaustion outcomes stated | Plan §4.9, §14.0, §12.4, §10.4 · R-10 · D-54 |
| **W4-11 (W5 fixer)** | **The cross-day resurgence rule existed only in the evidence base.** The dedupe index "remembers trajectory and prior-pack state" and the digest promises "what changed since a prior appearance" — with the rule mapping one to the other stated nowhere in the plan, and no producing stage, cost or checkpoint for the judgment | Two equally plausible implementations, and either could get built: blanket cross-day suppression by cluster key, silently killing legitimate follow-up coverage of a developing story; or blanket regeneration whenever a topic re-ranks, which is the four-near-identical-packs failure the control exists to prevent. RA-1 asked for exactly this not to be hand-waved | B3 §6.2's trajectory × prior-pack-state table **imported as canonical plan text** at §2.8a, with prior-pack state collapsed to the observable three values (never-generated / generated / rejected) because *published* lives inside the publishing bridge; a **rejected-topic suppression window** with a corroboration-growth override; the new-angle judgment named as **node N-2** with its stage, bounded input, cost and checkpoint; Phase-1 acceptance exercises all three states | Plan §2.8a, §1.2, §2.7, §12.1, §17 Phase 1 · D-40 |
| **W4-12 (W5 fixer)** | **Silent language thinness.** Theme-readiness asserts a *non-empty* candidate set per language, at theme-load time only. A language can pass that bar forever while producing one weakly-evidenced candidate a week | Czech becomes a slow-bleed language: technically valid, publishable by operator choice, commercially anemic, and invisible because nothing ever fails — dissolving into weekly operator decision fatigue rather than surfacing as a pattern. D-02's first-class guarantee fails quietly a second way, alongside the arithmetic route W2-07 already names | **Per-language minimum evidence-and-volume band**, evaluated at **every run** rather than at theme load, default deliberately loose so it does not cry wolf during cold start, breaching it raising a named digest line and escalating under the anti-flap rule. Never blocks a run, never relaxes a threshold to manufacture volume. The remedial ritual is the weekly curated-inbox session already committed at W2.5-2, not a new obligation | Plan §2.7, §12.1, §10.2 · R-22 · D-51 (T-3) |
| **W3-01a (W5 fixer) — SUPERSEDES the sizing in W3-01's mitigation** | **Theme-configuration surface breadth, correctly sized.** The resolved knob surface is **roughly 130 settings**, not the "roughly ninety" W3-01 sized its mitigation against — a ~30% understatement in the very row whose mitigation depends on the number. Separately, four knobs sat in the theme block that describe a machine rather than a tenant, and two sections contradicted each other on pacing and download-queue placement | A mitigation sized against the wrong problem, for the risk the log itself ranks as more likely than any provider or source failure; a copied theme inheriting another machine's disk threshold; and pacing that one tenant could loosen for everyone | The safe-by-default half of W3-01's mitigation stands unchanged. Added: a **machine/runtime block** holding the four machine-scoped knobs; pacing and download-queue policy resolved to **engine level**; and — the half that was missing — an **eleven-decision minimum viable knob set**, so a new theme author faces eleven decisions rather than a table of 130, with readiness validation naming which is absent. A genuine §15 risk row now exists for this risk (**R-35**) with an honest detection column stating plainly that no machine detection exists for a theme that is coherent but mistaken | Plan §10.1, §10.4a, §10.5, §13.1, §15.2 R-35 · D-47 |

## Wave 6 — risks opened by the operator's no-manual-inputs amendment (appended 2026-08-06, W6-1)

*Append-only. W6-1 abolished the weekly curated-inbox ritual and re-sourced Reddit. Row W2-01 is **retired** with the ritual it monitored — its failure mode (a skipped ritual degrading silently) can no longer occur because nothing depends on a ritual. These rows replace it.*

| ID | Risk | Consequence if ignored | Mitigation established | Owner / section |
|----|------|------------------------|------------------------|-----------------|
| **W6-01 (conductor)** | **Reddit commercial-API approval uncertainty.** The official Data API route requires a commercial-use approval Reddit may refuse, ignore, or answer slowly; B1 rated the surface the most litigation-active on the do-not-scrape list, so no unofficial route exists as a pressure valve | Phase 0 stalls on a third party's inbox, or an implementer "temporarily" reaches for the deprecated JSON endpoints — the exact violation F-9 exists to prevent | **Dual-track by design (W6-1):** the DataForSEO search-index fallback runs from day one and does not wait on Reddit; the application is filed in week 1 with an operator-set answer deadline (OD-29); the do-not-scrape list is restated as binding regardless of approval outcome | Phase 0 · OD-29 · F-9 |
| **W6-02 (conductor)** | **Search-rank is not virality.** The fallback route sees Reddit through a search index: rank, freshness and snippet — no upvotes, no comment velocity, no community context. A thread that ranks well but is not hot can masquerade as a trend | Overweighted Reddit candidates crowding out better-evidenced topics in the composite, precisely because the confidence axis was fed a number that looks like measurement | Fallback items enter as **low-confidence evidence, never measured virality** — the confidence multiplier discounts them structurally (same rule §2.5 applied to human-asserted items); the digest's per-source notes name the route so the operator can see which Reddit signal fed a pick | §2.5, §12.1 |
| **W6-03 (conductor)** | **The Czech thinness alarm lost its designed remedy.** W4-12's volume band kept its detection but its remedial action *was* the ritual D-51 pointed at; the band can now only escalate, not cure | Czech slow-bleed returns in a new form: the named digest line fires repeatedly, the operator has no scripted response, and alarm fatigue sets in — the W3-01 failure shape arriving through a source gap instead of a config error | The band's escalation now points at a **config-level remedy menu** instead of a ritual: enable additional CZ sources, widen Virlo CZ views, raise DataForSEO CZ keyword coverage — all config actions, consistent with the no-manual-inputs directive. Residual risk (a market where no automated CZ source carries the community signal) is **accepted by the operator** with W6-1 | §2.7, §12.1 · D-51 as amended |
