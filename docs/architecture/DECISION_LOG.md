# Decision Log — HypeAgentSocials design phase

*Seeded Wave 0 (2026-08-05). Append-only per wave; single writer per wave (see masterplan aggregating-files table). Each entry: status (LOCKED / OPEN / RESOLVED), owner wave, decision, rationale.*

---

## Locked decisions (from the grill, binding on all design work)

| ID | Status | Decision | Rationale |
|----|--------|----------|-----------|
| D-01 | LOCKED | Design phase only; implementation is a separate future plan after human approval | Assignment Stages 5–6 boundary |
| D-02 | LOCKED | Output languages = per-theme config array; every listed language gets its own full, first-class output set (first theme: `cs` + `en`); never a translation pass. Every architecture layer states its cs behavior explicitly | User decision; F-7 (Czech ≠ translated English) |
| D-03 | LOCKED | Brand truth: Notion MCP primary → theme config → live public website verification. Degrade honestly; unattended runs downgrade to research-only below a confidence threshold | User confirmed Notion MCP available; assignment fail-closed mandate |
| D-04 | LOCKED (amended W0.5) | Accounts: Kie.ai trial exists with **$50 credits** (model choice open — T2 recommends best image-gen + video-gen models). **No Postiz account yet** — operator trials it later; priority = prove research→assets pipeline first, distribution is a later phase. Higgsfield = paper evaluation only (confidence-tagged output + open decision). No paid official X API | User account reality, updated at W0.5 fact intake 2026-08-05 |
| D-05 | LOCKED | Runtime: Windows-first console app (run.bat + Task Scheduler) now; Linux server cron later → cross-platform mandatory | User deployment reality |
| D-06 | LOCKED | Tech stack: architecture plan recommends with justification + rejected alternatives (Python = working hypothesis; verified by brief C2) | User delegated ("go with what's best") |
| D-07 | LOCKED | Review UX: human reviews packs manually in an organized output folder; later optional config-driven upload to Notion. Operator = solo, marketing-literate, limited patience for file archaeology | User decision |
| D-08 | LOCKED (closed W0.5) | X for v1: no paid X reads, **reseller REJECTED by operator (2026-08-05)** — X reads are skipped/degraded-optional in v1. X-as-publish-destination remains a separate later-phase decision (F-2) | Pre-decided + operator answer at W0.5 fact intake |

## Open decisions (to be resolved at W0.5 / W2.5 gates or in §16 of the architecture plan)

| ID | Status | Decision needed | Gate | Notes |
|----|--------|-----------------|------|-------|
| OD-1 | RESOLVED (W0.5) | X reseller rejected → X reads skipped in v1; X only as degraded/optional free-surface source if any legitimately exists | — | D-08 closed. Reseller ToS/retention questions moot for v1 |
| OD-2 | OPEN | Reddit stance: licensed/aggregator path vs human-curated input vs drop for v1 | W2.5 (human) | F-1: ToS prohibits commercial API use; OAuth approval manual 2–4 wks; public JSON same-ToS + CDN-blocked. Only real options may be presented. Note: `GojiBerry_Reddit_01.txt` transcript = practitioner evidence of Reddit's value in this niche |
| OD-3 | OPEN | Higgsfield role: complement, competitor, or ignore for v1 | W2.5 (human) | F-6; paper-only eval (D-04) must output confidence + options |
| OD-4 | OPEN | Notion access split for cron: MCP (interactive) vs direct REST internal-integration token (unattended) — or MCP-only if auth reality allows | W2.5 (informed by T9) | Assignment says "MCP-extractable"; unattended-cron auth collision must be argued honestly |
| OD-5 | OPEN | Stack confirmation (Python working hypothesis vs alternative) | W2.5 (informed by T10) | D-06 delegates recommendation to the plan |
| OD-6 | RESOLVED (W0.5, 2026-08-05) | Fact intake answered: **Kie.ai = $50 credits**, model roster choice open — operator wants a recommendation for best image-gen + video-gen models (→ T2). **Postiz = no account yet**, trial later; prove research→assets first. **Notion = KB about HypeDigitaly + projects exists, MCP-connectable** (→ T9/T14). **Meta business accounts + LinkedIn company page both exist.** **GojiBerry_YoutubeInspiration files are real content** (4 GTM/growth transcripts, ~150 KB) — analyze meticulously (→ T1/T5/T8/T13) | — | Postiz draft-without-schedule capability remains empirically unverifiable until an account exists → paper research + mandatory fallback ladder (T9), logged as risk OP-2 |

---

## Wave 2.5 — operator decisions at the blocking checkpoint (appended 2026-08-06, conductor)

| ID | Status | Decision | Notes |
|----|--------|----------|-------|
| W2.5-1 | RESOLVED | X stays OUT of v1 despite new first-party pay-per-use option (~$50/mo). OD-7 closed; D-08 stands | Operator choice, recommendation followed |
| W2.5-2 | RESOLVED | Reddit v1 = Reddit Pro + weekly human-curated inbox ritual (~1 hr/wk) APPROVED. OD-2/D-09 closed | Operator committed to the ritual; staleness flag designed in |
| W2.5-3 | RESOLVED | Trend-vendor spend APPROVED: Virlo.ai Starter $49/mo gated on a 1-week trial (niche-fit check; Shortimize = fallback) + DataForSEO ~$10–15/mo. Total ~$60–65/mo | Virlo trial outcome remains an implementation gate |
| W2.5-4 | **RESOLVED — synthesis recommendation OVERRULED** | D-02a REJECTED: operator chose **IDENTICAL asset mixes in both languages** (cs gets TikTok/Reels/Shorts in v1). D-02 stays literal. Production recipes still differ per language (cs video = TTS voice-over / carousel-to-reel; model-native cs speech stays banned per A4/C6) — identical MIX ≠ identical recipe. Doubled media spend accepted (OP-1 stands) | Operator choice against B4×A1×A4 lean; architecture must make cs short-form NOT look cheap (B4's warning becomes a design obligation, not a mix reduction) |
| W2.5-5 | RESOLVED | Stack = **Python** confirmed. OD-5/D-06 closed | Per C2 verification |
| W2.5-6 | RESOLVED | Higgsfield = ignore for v1 (no pipeline role; fal.ai stays registered fallback router). OD-3 closed | Per A2 H1 recommendation |
| W2.5-7 | RESOLVED | Run cadence = **config knob, default OFF** (no runs until operator enables), with daily/weekly options; exact knob design (research cadence vs pack cadence, per-theme) is an architecture §10 task — conceptual design, no syntax | Operator: "settable in config — default off, otherwise daily/weekly; figure out the best way" |
| W2.5-8 | RESOLVED | Notifications = **email** ("packs ready" + failure alerts) | Recommendation followed |

Defaults adopted without a user gate (operator may veto at Stage 5): D-10 Notion split (REST internal token for cron/records, MCP for interactive); ElevenLabs primary cs TTS w/ Azure fallback; local FFmpeg assembly; 30-day raw-artifact retention default; DataForSEO as SERP vendor; human-reviewed periodic ranking calibration (no auto weight updates); Postiz trial deferred to implementation start; blog pipeline config-gated, deferred detail to implementation.

## Wave 4 — conductor triage of review findings (appended 2026-08-06, conductor)

Review totals: R1 3B/13M/7m · R2 4B/19M/9m · R3 1B/6M/5m · R4 5B/9M/7m · R5 4B/13M/6m (B=blocker, M=major, m=minor).

**Triage outcome: ALL 17 blockers ACCEPTED; ALL majors ACCEPTED.** Rationale: every blocker/major was verified against the plan text as a genuine contradiction, gap, or unimplemented obligation; none conflicts with a locked or operator decision; the reviews' "required change" columns are the fix specifications. Minors: fixer applies where cheap, else logs to Appendix B with rationale. Notable triage directions (where a finding offered options, the conductor picks):

| # | Finding | Direction |
|---|---------|-----------|
| T-1 | R4-B1 (likeness/voice consent absent) | Take the BAN option: v1 explicitly disables synthetic human presenters and voice clones (logged decision); person-basis flag + consent-record design deferred until avatars enter scope. Constrain the "talking human" motion class + voice-identity knob accordingly |
| T-2 | R4-B2 (AI-content class relaxable by config; audio omitted) | Engine-level derivation from the generation record; config may only tighten; extend to synthetic speech + music (CS-B TTS assets ARE synthetic-audio) |
| T-3 | R3-B1 (Czech thin-pack silent degradation) | Take option (a): per-language minimum evidence/volume band knob, default loose, alarmable in digest; Czech curation ritual stays merged with the committed weekly Reddit ritual (W2.5-2), surfaced by the staleness flag |
| T-4 | R1-M12 (voided SYNTHESIS table still live) | EXCEPTION authorized to the single-writer rule: fixer may APPEND supersession banners to SYNTHESIS.md §3.1 and §7 item 8 pointing at W2.5-4/D-27 (append-only; logged here) |
| T-5 | R1-B3 (Appendix A contradicts test-mode dry-run default) | Re-cast per option (a): explicit dry-run-disabled statement + why, plus the §4.6 one-sentence rule (mode never authorizes spend; the dry-run flag converts forecast→submission) |
| T-6 | R1-B1 + R2-B1 (text-model layer: no provider role, no token budget, no economics) | Treat as ONE structural fix: text-provider role in §5, LLM budget block (per-run ceiling + per-stage call caps + judge allowance) consumed by the cost gate, text line in §5.4 economics + §12.1 forecast |
| T-7 | R5-B2/F13 + R5-F14/F16 (caps units incompatible) | Canonical unit = MASTERS per language; rewrite §8.11's example in masters; specify count-cap vs dollar-cap precedence (whichever trips first stops submission; both logged) |
| T-8 | R4 majors OD-24/25/26 | Add to §16 as named open legal items; EDPB-guideline verification becomes a Phase-0 prerequisite |
| T-9 | R2-M (price re-verification lapse = self-inflicted outage) | Soften lapse behavior: stale price data → forecasts marked stale + submission requires operator acknowledgement, not hard pipeline stop |

## Wave 2 synthesis — refinements to locked decisions (appended 2026-08-06, T16)

*Refinements narrow or clarify a locked decision; they never reverse one. Each cites the owning brief.*

| ID | Status | Refinement | Basis |
|----|--------|------------|-------|
| D-02a | REFINED — needs W2.5 confirmation (W2 synthesis) | D-02's "full first-class output set" is interpreted as **equal-quality, per-language-appropriate asset mixes, not identical asset mixes**. Binding parts unchanged: never a translation pass; every configured language gets a fully designed, quality-gated set, asserted by theme-readiness validation. The cs set excludes TikTok in v1 and uses the carousel-to-reel recipe; the en set is generative-clip-led | B4 (short-form perception risk in CZ B2B) × A1 (3+/week video bar) × A4 (carousel-to-reel is the safest cs video format) — SYNTHESIS §3.1 |
| D-03a | REFINED (W2 synthesis) | Brand-truth precedence is **per fact class**, not one flat order. The **live site wins on commercially binding facts** (price values, trial terms, plan contents, guarantees, offer availability, CTA URL liveness); Notion is primary on internal knowledge; config is primary on design artefacts. Three asymmetries bind: excludes are monotonic; the site may invalidate a claim but never add one; silence ≠ agreement and a failed fetch ≠ disagreement. Disagreement inside the commercially binding set is a red-flag stop, never a tie-break | C6 §4 — SYNTHESIS §3.3 |
| D-04a | REFINED (W2 synthesis) | Kie.ai is the v1 **media router** (primary route host), treated as a replaceable non-SLA dependency; fal.ai is the registered fallback router; a direct Google API migration path is documented. Higgsfield recommendation: ignore for v1 as an API route (H1), optional personal seat outside the pipeline (H2) — OD-3 carries the recommendation | A2 §2.1–2.4 |
| D-08a | UNCHANGED, new fact recorded (W2 synthesis) | D-08 stands for v1. Recorded for the W2.5 table: X's **first-party** pricing became pay-per-use on 2026-02-06 at $0.005/read (2M/mo cap) ≈ $50/mo at research volumes. This is not a reseller and post-dates the framing in which D-08 was decided. No decision taken here — presented as OD-7 | B1 §3.2, B2 §2.9 — SYNTHESIS §3.5 |

## Wave 2 synthesis — new resolved decisions (appended 2026-08-06, T16)

| ID | Status | Decision | Basis |
|----|--------|----------|-------|
| D-09 | RESOLVED-RECOMMENDED → W2.5 sign-off (W2 synthesis) | Reddit v1 = **Reddit Pro + weekly human-curated inbox; no Reddit API, no scraping, ever**. "Curated human input" becomes a first-class source type with its own staleness flag. Enterprise API deferred with trigger = multiple paying tenants. Closes OD-2 pending sign-off | B1 §3.1 + B2 §2.2 + C7 §2.1 (three-way convergence) |
| D-10 | RESOLVED-RECOMMENDED → W2.5 sign-off (W2 synthesis) | Notion access split: **REST with a non-expiring internal integration token for every pack-bearing resolution including all cron runs; hosted MCP for interactive exploration only**. "MCP is for humans, REST is for records." Also required to enforce "offers not explicitly `live` are unspinnable" (property filtering MCP lacks). Closes OD-4 pending sign-off | C1 §2 (hosted MCP tokens expire ~3h; REST tokens non-expiring, full property filters) |
| D-11 | RESOLVED-RECOMMENDED → W2.5 sign-off (W2 synthesis) | Stack = **Python**, managed project checkout with locked dependencies and a thin per-OS launcher; scheduled runs execute under the operator's own account, never SYSTEM; explicit UTF-8 everywhere. Node/TS, single-file packaging and .NET rejected with rationale. Closes OD-5 pending sign-off | C2 §2.5–2.8 |
| D-12 | RESOLVED (W2 synthesis) | **No Playwright / browser automation in the v1 collection path.** The do-not-scrape list is binding in all modes including degraded ones. Playwright remains only as (a) a per-source explicitly-approved future exception mechanism, (b) verification fetches against our own public pages, (c) QA screenshots | B2 §2.1–2.4, C7 §2.3 |
| D-13 | RESOLVED (W2 synthesis) | Model roster: images **Nano Banana 2** default + **Nano Banana Pro** for text-carrying finals (Seedream 5 Pro / GPT Image 2 fallbacks); video **Veo 3.1 Fast** workhorse + **Veo 3.1 Quality** hero-only + **Seedance 1.0 Lite** draft tier; **Kling 3.0** registered alternate and the only multi-shot route (hero tier only). **Sora 2 excluded everywhere** (API removal 2026-09-24). **Suno-via-Kie forbidden** for published assets | A2 §2.2, §2.1.5 |
| D-14 | RESOLVED (W2 synthesis) | **One en/cs video pipeline with the fork at the audio-sourcing stage only.** Three legal recipes: EN-A generative clips + native English speech; EN-B/CS-A generative clips + TTS VO; CS-B (cs default) slide-motion carousel-to-reel + Czech TTS or subtitles+music. **Model-native Czech speech banned in v1**; model-rendered message-bearing text banned in both languages; fail-closed to subtitles-only, never to English audio | A2 §2.5/2.7 + A4 §2.5/2.6 + C6 §8 — SYNTHESIS §3.2 |
| D-15 | RESOLVED (W2 synthesis) | **The exemplar corpus is style-only and never a fact source.** Excluded from all factual retrieval paths; the claim ledger is never populated from it; a **corpus-leakage check class** blocks generated numbers, metric phrases and entities that appear in the corpus but not in the claim ledger, and reports leakage events | C6 §7.4, C5 §3 |
| D-16 | RESOLVED (W2 synthesis) | **Claim gate runs twice** — pass 1 fail-fast, pass 2 as the final immutable gate on the exact packed bytes (because the voice gate rewrites text). Deterministic extraction precedes semantic checking. **Script-lock is the primary control for spoken claims**; unattended spoken lines carry zero claim tokens; ASR is a sampled adherence monitor, never a per-asset gate | C6 §7.3, §8 |
| D-17 | RESOLVED (W2 synthesis) | **All media money-safety is client-side: no provider idempotency exists.** Write-ahead spend ledger with deterministic asset identity committed before submission; restart resolves by querying task status, never blind resubmission; a named `submitted-unknown` state with no auto-resubmit; immediate re-host of every artifact (14-day provider deletion) with checksum verification; expiry-ordered download queue drained before new submissions; balance-delta reconciliation with an unexplained-spend circuit breaker; delivered-vs-requested route/aspect recorded (silent model substitution is documented) | A2 §2.6/2.8 re-derived against C3 §2.3 — SYNTHESIS §2b. **Corrects C3's "client-supplied idempotency token passed to the provider"** — no such field exists |
| D-18 | RESOLVED (W2 synthesis) | **Exit-code taxonomy = nine named classes**: success · completed-with-pending-media · partial-success (degraded sources) · partial-success (budget-capped mid-pack) · completed-degraded (research-only) · budget-stop (pre-emptive) · policy-stop · skipped-overlap · hard-failure. Numeric mapping deferred to implementation; detail lives in the run ledger | C3 §2.6 + A2 §2.6 (pending media is a healthy outcome) + C6 §5.5 (degrade is a distinct outcome) |
| D-19 | RESOLVED (W2 synthesis) | **Every AI-generated video/image/audio asset carries a burned-in, human-perceivable AI disclosure applied at render time**, and cannot be marked publish-ready without it. C2PA is signed after final encode and archived with the pack but is **never** the compliance mechanism — platforms strip manifests on re-encode. Platform-native labels are a separate cumulative obligation set at publish-prep, manually in v1 (Postiz exposes no label fields) | C7 §2.4/2.4a + A4 §2.9 + C1 §7 |
| D-20 | RESOLVED (W2 synthesis) | **Per-asset provenance record is a publish-gate precondition**: delivered route identity and version, generation timestamp, a snapshot of that model's commercial-use terms at generation time, and the router transaction id. Resolved *after* completion, not at submission, because the router may substitute a model with a different rights class | C7 §2.8 (OpenRouter/Replicate structural analogy) × A2 §2.7 (silent fallback) |
| D-21 | RESOLVED (W2 synthesis) | **Canonical vocabulary is fixed in SYNTHESIS.md §4 and is normative.** Later authors may not invent new nouns for named stages, artifacts, modes, gates, ledgers, config blocks, exit-code classes, provider roles or the two lists. Listed synonyms are to be avoided | T16 consolidation across all 16 briefs |
| D-22 | RESOLVED (W2 synthesis) | **Ranking re-derived against real source findings**: three evidence classes (counted / ranked / human-asserted) replace one virality model; corroboration counts across seven **source families**, not source instances; baselines are bounded (90 days generally, 30 for YouTube) with an absolute-band fallback for the first ~14 days; a fifth signal class (ad-creative-pattern) has an **inverted** age term; **the cs composite drops the virality factor entirely** because no Czech-signal carrier exposes engagement counts | B3 re-derived against B1 §2/§4 and B2 §2.2/§2.8 — SYNTHESIS §2a |
| D-23 | RESOLVED (W2 synthesis) | **The publish gate is the single fail-closed enforcement point** reading the publish allowlist per mode; research-only platforms may never appear in any allowlist and are never connected as channels at all; an allowlisted-but-unconnected channel fails closed and is named in the pack — no silent skip, no silent substitution. Five defence layers total (F-4) | C1 §9 |
| D-24 | RESOLVED (W2 synthesis) | **Assembly is a named pipeline stage owned locally**: FFmpeg-core engine of record with a cloud assembly API behind an adapter seam; all message-bearing on-screen text in both languages applied post-render with bundled Czech-glyph-complete fonts; caption text always from the authored script with alignment supplying timing only; −14 LUFS / −1.0 dBTP mastering logged per asset as a QA gate; ≈900×1400 universal safe box; licensed music only | A4 §2.1–2.8 |
| D-25 | RESOLVED (W2 synthesis) | **Buy-on-top where platforms are closed; build only what is free and officially open.** Short-form trend axis (TikTok/Reels/Shorts) is bought from a licensed trend-intelligence vendor, not built. Licensed data standardises on **MCP as the integration idiom** (a first-class "MCP source" connector class with a credit budget, async job semantics and vendor-churn resilience) alongside owned collectors and the curated inbox | B5 §3.2/§4.1 — SYNTHESIS §3.13 |
| D-26 | RESOLVED (W2 synthesis) | **Czech voice rubric exits synthesis concrete, not a framework**: calque blocklist with named native alternatives, structural AI tells, a code-switching allowlist (English tool/metric/category nouns permitted; English-rooted verbs and abstract benefit nouns blocked), an eleven-dimension judge rubric with **vykání as the default register**, and a Czech soft-CTA phrase bank mapped to CTA classes. B4's internal §1-vs-§7 formality conflict resolved in favour of vykání-by-default | C5 rubric structure filled from B4 §1/§5/§6/§7 — SYNTHESIS §2c |

## Wave 2 synthesis — new open decisions for W2.5 (appended 2026-08-06, T16)

*Full options and recommendations in SYNTHESIS.md §7.*

| ID | Status | Decision needed | Gate | W2 recommendation |
|----|--------|-----------------|------|-------------------|
| OD-7 | OPEN (W2 synthesis) | Reopen X reads on the new first-party pay-per-use fact ($0.005/read ≈ $50/mo)? Not a reseller; post-dates D-08's framing | W2.5 (human) | Keep closed for v1; measurable v2 trigger = share of packs anchored on sub-24h signals near zero after 4 weeks |
| OD-8 | OPEN (W2 synthesis) | Cadence and per-run caps | W2.5 (human) | Daily collection run (no media) + 3 pack runs/week; ~5 topics/run; **1–2 media-bearing assets per language per run** (video QA is the bottleneck, not topics) |
| OD-9 | OPEN (W2 synthesis) | Claim-ledger location | W2.5 (human) | Notion typed database primary + config pointer and policies; **hard excludes duplicated in config** (monotonic, must survive a Notion outage — union wins). Resolves C6 D-d |
| OD-10 | OPEN (W2 synthesis) | FFmpeg distribution and GPL/LGPL build choice | W2.5 (human) | Require a managed install of a pinned version per OS; do not distribute binaries in v1; bundle fonts, not codecs. Resolves A4 D3 |
| OD-11 | OPEN (W2 synthesis) | Notification push channel | W2.5 (human) | Filesystem status flag mandatory baseline + email default; chat webhook as a config alternative; desktop notification rejected as primary |
| OD-12 | OPEN (W2 synthesis) | Demand-axis vendor: adopt DataForSEO or manual CSV only | W2.5 (human) | Adopt, as an explicitly logged vendor-risk decision (C7: Google's official low-friction API is closed to new customers); manual Trends CSV retained as the designed degraded state; prefer DataForSEO over SerpApi on character grounds |
| OD-13 | OPEN (W2 synthesis) | Czech TTS primary | W2.5 / trial | ElevenLabs primary (production evidence in cs), Azure Neural cost/fallback tier; A/B on real cs scripts during the trial. Resolves A4 D2 |
| OD-14 | OPEN (W2 synthesis) | Blog/site path scope for v1 | W2.5 (human) | Drafts only — article produced as a pack artefact at FULL band; no site-publishing integration; the site-first hold is released by the operator publishing manually |
| OD-15 | OPEN (W2 synthesis) | Retention windows and personal-data handling for research artifacts | W2.5 + counsel | Adopt B2's defaults (raw 30d / normalised 90d / request log 12mo / provenance permanent with pack); curated-inbox verbatim text 30d with hashed author keys; targeted deletion by canonical key from day one |
| OD-16 | OPEN (W2 synthesis) | **Trend-intelligence vendor spend (~$60–65/mo) + the Virlo one-week trial** | W2.5 (human) | Approve, trial-gated. Trial must answer: (1) is full MCP/API access included at Starter — vendor's own guide and pricing page **conflict**, and this gates adoption; (2) does an AI/B2B niche monitor beat the free portfolio; (3) what are the unpublished rate limits. Fallback: Shortimize at the same gate, then the monthly Creative Center ritual as a designed degraded state |
| OD-17 | OPEN (W2 synthesis) | C7 follow-up: legality posture of **derived-analytics** vendors whose upstream collection is undisclosed and unlicensable (Virlo class) versus raw scraped passthrough (excluded class) | W2.5 → C7/R4 | Working assumption: permitted class, same posture as Exploding Topics/BuzzSumo. Deserves one paragraph of legal reading, not a shrug |
| OD-18 | OPEN (W2 synthesis) | Google Alerts RSS availability (no longer documented in current help) | Implementation setup | Design email-to-inbox ingest as the default (needed for newsletters anyway); treat RSS as a bonus if the option still appears at setup |
| OD-19 | OPEN (W2 synthesis) | Product Hunt commercial-use characterisation (unaddressed by C7) | W2.5 / operator | Use public pages and RSS until a permission email is answered; the API is not load-bearing |
| OD-20 | OPEN (W2 synthesis) | Judge/threshold calibration governance | W2.5 (human) | Human-moved thresholds only in v1; monthly calibration report produced by the system and applied by a human; loosening the brand-fit floor requires a logged rationale |

## Wave 3b — decisions taken while assembling the architecture plan (appended 2026-08-06, T19 assembler)

*These close items the Wave-3 drafts explicitly surfaced for the assembler. None reverses a LOCKED or RESOLVED row; each re-derives a consequence of one. All are marked "(W3b assembler)" and are reflected in `ARCHITECTURE_PLAN.md`.*

| ID | Status | Decision | Rationale / where it lands |
|----|--------|----------|----------------------------|
| D-27 (W3b assembler) | RESOLVED | **D-02a is formally SUPERSEDED by W2.5-4, and `SYNTHESIS.md` §3.1's per-language channel table is void.** Czech gets TikTok, Instagram Reels and YouTube Shorts in v1; the identical destination × asset-type matrix applies to both configured languages. D-02 stays literal | The operator overruled the synthesis recommendation at W2.5-4; the refinement that recorded the old interpretation cannot stand beside it. Plan §0.2, §3.1, §3.2, §12.2 |
| D-28 (W3b assembler) | RESOLVED | **W2-08's recorded mitigation is replaced.** "TikTok excluded for cs in v1" is void; the replacement mitigation is the **six Czech design commitments** — recipe not translation, framing not mimicry, register discipline, understatement as a quality bar, a destination-aware production floor, and a measurable revisit trigger | B4's evidence did not disappear when the mix changed; it converts from a mix reduction into a design obligation. Plan §3.1, §15.4; superseding row appended to `RISK_LOG.md` |
| D-29 (W3b assembler) | RESOLVED | **Media-bearing caps count masters produced per language, not destination derivatives.** One 9:16 master serves TikTok, Reels and Shorts by layered re-composition; a derivative costs a re-render, not a generation | OD-8's recommendation predated the identical-mix decision and was ambiguous under it. Plan §3.2, §10.4, Appendix A.5 |
| D-30 (W3b assembler) | RESOLVED | **X as a publish destination is config-gated and default-off; X assets may still be produced at no marginal cost, and X is never a connected channel until an explicit decision.** Whether to suppress X assets entirely is carried as OD-21 | Read path and publish path are independent (F-2). Plan §3.2, §7.5, §16 |
| D-31 (W3b assembler) | RESOLVED | **The conclusion "the Czech mix is cheaper, which softens OP-1" is CONDITIONAL, not general** — true while Czech runs the CS-B carousel-to-reel recipe, false the moment Czech is promoted to generative clips. No cost figure is hard-coded anywhere; the forecast reads the model registry's price snapshots with the snapshot date displayed | The synthesis sentence was written under the per-language-mix assumption D-27 voids. Plan §3.1, §5.4, §15 R-11 |
| D-32 (W3b assembler) | RESOLVED | **OD-9 (claim-ledger location) is carried through the plan as a recommendation, not a lock.** The plan assumes the Notion-primary split with hard excludes duplicated in config and the union winning; if the operator chooses config-only, monotonicity survives but claims can no longer be edited without touching configuration | Keeps the open decision genuinely open while letting the design be concrete. Plan §6.3, §16 OD-9 |
| D-33 (W3b assembler) | RESOLVED | **Single-owner rule for repeated material.** Each subject that spans sections has exactly one home and the others point to it: spin criteria S-1…S-7 in §6.10; the eleven claim check classes in §6.7; the spoken-claim rationale in §6.8; the mode capability matrix and its resolver in §11.1–§11.2; ledger substrates in §8.6; the source portfolio in §2.3; per-stage idempotency in §8.5 | Prevents two sets of words drifting apart across a 370 KB document. Plan §0.2 |
| D-34 (W3b assembler) | RESOLVED | **The two Czech prerequisites are scheduled, not assumed.** The Czech structural-calibration corpus and the Czech judge golden set are Phase-0 deliverables with their own acceptance criteria and a hard do-not-start-Phase-1 gate | Neither the sentence-length-variance band nor the judge threshold transfers from English (F-7). Plan §17 Phase 0 |
| D-35 (W3b assembler) | RESOLVED | **The cost gate and the publish gate both read the single mode capability resolver rather than encoding mode logic of their own**, and this is stated at every gate mention across §5, §6, §7 and §11 | RA-8 asks for one choke point; two specialised enforcement points acting on one resolver's answer is that, provided the plan says so everywhere. Plan §5.4, §6.5, §7.4, §11.2 |

### Wave 3b — new open decisions (appended 2026-08-06, T19 assembler)

| ID | Status | Decision needed | Gate | Recommendation |
|----|--------|-----------------|------|----------------|
| OD-21 (W3b assembler) | OPEN | **X assets: produce-but-never-publish, or suppress entirely?** §3.2 produces them at no marginal cost while X is never a connected channel | Stage 5 (operator preference) | Produce them. Cost is a few hundred tokens; the upside is a back catalogue and a proven format profile the day X becomes a destination. Suppressing entirely is a one-knob change if the operator finds unused assets in the pack annoying |
| OD-22 (W3b assembler) | OPEN | **The Czech short-form revisit window.** D-28's sixth commitment promises a measurable revisit trigger but names no number | Stage 5, then the trigger itself | A volume threshold (for example, twenty published Czech short-form assets) rather than a calendar window, with a twelve-week backstop. Volume measures the evidence the operator overruled research to obtain; the calendar measures nothing |
| OD-23 (W3b assembler) | OPEN | **The fallback-router engagement threshold.** §5.7 names every migration rung but deliberately leaves this trigger unset rather than guessing | After four weeks of real run data | A reliability threshold (degraded runs attributable to the primary router in a rolling window) as primary, with a monthly-spend threshold as secondary. Integrating a second live path now is rejected — it doubles the money-safety surface while the first router is still unproven |

---

## Wave 5 — decisions created by applying the Wave-4 review findings (appended 2026-08-06, T25 fixer)

*Every row below is marked **(W5 fixer)** and follows from an accepted blocker or major in `docs/reviews/`, applied per the Wave-4 conductor triage above. None reverses a LOCKED or operator (W2.5) decision; each closes a seam a review found open. All are reflected in `ARCHITECTURE_PLAN.md` and mapped finding-by-finding in its Appendix B.*

| ID | Status | Decision | Rationale / where it lands |
|----|--------|----------|----------------------------|
| D-36 (W5 fixer) | RESOLVED | **v1 creates and uses no synthetically created or synthetically altered human presenter, and no cloned voice.** The `talking human` motion class is a disabled value; no reference input depicting an identifiable person may be supplied to any generative route; only voice routes whose `voice-basis` is `licensed-catalogue` may be selected, and `cloned-real-person` is a forbidden rights class. The person-basis flag and the scope-limited consent record are **designed but out of scope until avatars are**, at which point they become prerequisites rather than follow-ups | Conductor direction T-1 on R4-B1. The plan had a provider *content policy* (no-people / adults-only / region-restricted) and treated it as a personality-rights control, which it is not — it would stop a generated public figure and not touch an employee "founder avatar", a licensed stock actor or a cloned voice. Ban is the cheapest correct answer and removes an entire consent regime from v1. Plan §5.3, §4.8, §4.10, §16 OD-27, R-38 |
| D-37 (W5 fixer) | RESOLVED | **The AI-content class is an engine-level field derived from the generation record, not a per-theme knob.** An asset is realistic-synthetic if **any** visual, audio or motion component was model-rendered — **including synthetic speech and generated music**. Configuration may only tighten the derived class, never relax it (the same monotonic rule as hard excludes) | Conductor direction T-2 on R4-B2. The old default read "realistic-synthetic for generated video and imagery", omitting audio — which landed exactly on **CS-B, the Czech default recipe** (TTS voice, no generative video model), so the largest volume of assets in the pipeline could legitimately class as `none` and skip the burned-in disclosure while Article 50(2) names synthetic audio explicitly. Plan §3.3, §10.4, §17 Phase 3 |
| D-38 (W5 fixer) | RESOLVED | **Masters per language is the canonical cap unit throughout the plan**, and its scope is *every* media-bearing master, not video alone — a 9:16 video master is one, a 9:16 slide-art set is one. **The count cap and the dollar cost gate are independent limits; whichever trips first stops submission, and both are logged distinctly** (*count-capped* and *budget-capped* are different digest lines) | Conductor direction T-7 on R5-F13/F14/F16. §8.11's mid-pack illustration counted destinations, which the masters model makes architecturally impossible — five of six destinations are free simultaneous derivatives of one master, so no dollar cap can trip between them. Appendix A.5's "masters produced: two" followed by four items was the same defect showing as arithmetic. Extends D-29 rather than replacing it. Plan §0.2, §3.2, §8.11, §10.4, Appendix A.5 |
| D-39 (W5 fixer) | RESOLVED | **A text-model provider is a first-class provider role, and text spend has its own budget block.** Two roles only — drafting and judge, ideally different lineages. The model registry carries four route classes (video, image, text, voice). **§5.4a's LLM budget block** sets per-run/day/month text budgets, per-stage call ceilings and a per-pack judge allowance, all enforced pre-call by the same cost gate at the same boundary as media; the digest shows media and text forecasts as two lines, never blended | Conductor direction T-6 on R1-B1 + R2-B1, treated as one structural fix. §1.5's cost-computability argument — the reason a deterministic pipeline was chosen over an agentic one — was only true for the media wallet; the text wallet had no role, no registry, no cap, no forecast and no line in the economics, against ~130 text artifacts per run each passing five model-mediated evaluations. Plan §1.5, §5.1, §5.2, §5.4, §5.4a, §8.11, §10.4, §11.1, §12.1 |
| D-40 (W5 fixer) | RESOLVED | **B3 §6.2's cross-day resurgence rule is canonical plan text**, with prior-pack state collapsed to what this architecture can observe — **never-generated / generated / rejected** — because *published* lives inside the publishing bridge and returns only through an optional manual path. A rejected topic is suppressed for a configured window unless corroboration grows by more than a configured number of families. The new-angle judgment is node **N-2**, in the ranking stage, one call per resurgence candidate, checkpointed with that stage | R5-F10 and R3-M4. The plan named the inputs (trajectory, prior-pack state) and promised the output ("what changed") without ever stating the rule between them, leaving two equally plausible implementations — blanket suppression or blanket regeneration — and making the mandated scenario question unanswerable. Plan §2.8a (new), §1.2, §2.7, §12.1, §17 Phase 1 |
| D-41 (W5 fixer) | RESOLVED | **Repair re-entry is a stated rule: a claim-pass-2 repair re-enters the voice gate and the spin artifact-level post-check before pass 2 runs again**, under a **single combined per-artifact repair ceiling** counting repairs across all gates, exhausting to the downgrade-repair variant rather than to another lap. **For media assets, a post-assembly overlay claim pass** (deterministic classes over the composed on-screen strings) plus template slot discipline makes "final on the exact packed bytes" true for pixels as well as text | R2-B3. §6.7 stated the principle — any gate before a rewriting step must re-run after it — and then broke it: a claim-2 regenerate is a rewriting step, so the asset that shipped had its slop control performed on a superseded draft. Separately, all claim-bearing on-screen text is composed at assembly, after pass 2 closes. Plan §6.10, §14.0, §4.4, Appendix A.7 |
| D-42 (W5 fixer) | RESOLVED | **Delivered route identity is best-effort with a defined inference rule, not an asserted field.** Three states: `identity-reported`; `substituted — identity unknown`, inferred from the documented substitution signature; `assumed-as-requested`, flagged as an assumption. **An asset whose renderer cannot be identified is not publish-ready and degrades to plan-only with the reason attached.** "Does the router's response name the rendering model" is a Phase-0 checklist item | R2-B4. D-20 made the field a publish-gate precondition; the evidence never established it is observable — the substitution is documented as *silent*, detectable only by side effects. Making a mandatory gate field from an unverified capability turns a rights defence into a publish deadlock. Plan §5.6, §7.4, §12.2, §17 Phase 0/3 |
| D-43 (W5 fixer) | RESOLVED | **The provenance snapshot and the pack's per-signal source records are split into a permanent de-identified part and a 30-day verbatim/permalink part**, the second expiring in place to a placeholder. **A run-pack → canonical-key index** is added to the ledger set so targeted deletion reaches inside archived packs. **The "links and excerpts, not payloads" justification is withdrawn as the wrong legal test.** Author handles use a keyed deterministic hash with the key held separately; handles and permalinks are redacted from prompt payloads | R4-B5 and R4-m1. The plan set a 30-day window on verbatim text three rows above a row that kept the same verbatim text permanently in the pack, and Phase 1 tested deletion against a live record but never against a packaged one — leaving Articles 17 and 21 unexecutable against every pack ever written. Plan §2.6, §2.6a, §8.6, §12.2, §17 Phase 1 |
| D-44 (W5 fixer) | RESOLVED | **The AI-label acknowledgement is emitted at packaging and travels with the artefact**, as an unticked per-platform checklist inside the rung-3 staging file. **The publish gate fires "before any distribution preparation", not "before any bridge call"** | R4-B3. Rung 3 makes no bridge call by definition, so the gate never fired on the one path that is always available and is explicitly where gate-blocked content lands. The burned-in disclosure was never at risk (it is a publish-ready precondition at assembly); what was skipped was the platform-contractual label layer and the single-enforcement-point claim itself. Plan §7.2, §7.4, §17 Phase 6, Appendix A.10 |
| D-45 (W5 fixer) | RESOLVED | **A fifth fail-closed trigger: any gate or judgment node that cannot execute fails its artifact closed to that gate's own named degraded outcome, with the gate named in the pack.** Per-gate outcomes are enumerated for the voice judge, the claim semantic pass, the spin gate, the keyframe rubric, the platform gate, the asset QA rubric and node N-2 | R1-M3 and R2-M17. The correct behaviour was specified once, for the brand-fit judgment, and nowhere else — so an unattended run against a degraded judge endpoint produced never-semantically-gated assets sitting beside gated ones. A gate that cannot run and therefore does not block is exactly the policy violation the unattended mandate exists for. Plan §11.3, §14.2, §4.2a |
| D-46 (W5 fixer) | RESOLVED | **The dry-run default per mode is engine-level, not a theme knob, and is enforced by the mode capability resolver.** The governing rule, stated at §4.6 and repeated in Appendix A's header: **a mode never authorises spend on its own; the dry-run flag is what converts a forecast into a submission** | Conductor direction T-5 on R1-B3, plus R1-M13. What actually made test mode cheap was a default sitting where a theme author could edit it, while §11.1's media-spend row read identically in all three modes — so the resolver had nothing mode-specific to say about the most expensive side-effect class. Appendix A is re-cast per option (a): the run states that dry-run is deliberately off, and why an operator would do that. Plan §4.6, §10.4a, §10.5, §11.1, Appendix A |
| D-47 (W5 fixer) | RESOLVED | **A machine/runtime block exists alongside the three theme blocks**, holding settings that describe the computer rather than the tenant: low-disk threshold and reserved ledger headroom, log verbosity and retention, secrets location and permission policy, launcher and interpreter path resolution, working and output roots. **Submission pacing and download-queue drain policy resolve to engine level**, and the knob surface is stated at ≈130 rather than "roughly ninety" | R1-M8. Four knobs in the theme's output/runtime block failed the plan's own placement rule outright (one's default column literally read "per machine"), §10.4 and §10.5 contradicted each other on pacing, and W3-01's mitigation was sized against a count that was 30% low. An **eleven-decision minimum viable knob set** is added as the operator-overwhelm half of that mitigation. Plan §10.1, §10.4a, §10.5; `RISK_LOG.md` W3-01a |
| D-48 (W5 fixer) | RESOLVED | **Assembly is a resumable unit of work with its own assembly ledger** (blocked-on-inputs → assembling → assembled → failed), unblocked when its last media-job input reaches `done`. **Assembly is not spend-bearing, so any run may complete a blocked master** — including a collection-only run. **Pack amendment is explicit**: the adopting run reopens and amends the originating pack under an incremented revision, writes an amendment record into the review-decision store against the original run id, and cross-references it in both digests. Until assembled, a pending master is packaged as a plan-only artifact with its completed clips attached and a days-to-deletion countdown | R2-B2. *completed-with-pending-media* was a headline healthy outcome with nothing designed to finish the asset: assembly consumed several media-job rows and had no ledger row of its own, packaging's idempotency key broke on completion, and the adopting run's mode and budget context were undefined. Appendix A demonstrated the gap by shipping a mastered, loudness-gated, reviewed master whose clip was still rendering. Plan §4.7a, §8.6, §8.13, Appendix A |
| D-49 (W5 fixer) | RESOLVED | **A global cross-theme media-spend lock plus global daily and monthly caps** sit above the per-theme ones, because balance-delta reconciliation is only interpretable when one process spends against the balance. The alternative the operator may choose instead is **one router sub-account per theme**, recorded as a real onboarding cost for theme #2. "A separate router **key** with a bounded top-up" is corrected to "a separate router **account**" — a top-up limit is a property of an account, not a key | R2-M12 and R2-m2. The run lock and both cadence knobs are per theme while the wallet is singular, so two themes could legitimately run concurrently and make the unexplained-spend circuit breaker either fire falsely or mask real spend, with N × cap total exposure and no global ceiling. Plan §5.4, §8.11, §13.1 |
| D-50 (W5 fixer) | RESOLVED | **There is no unattended offline-snapshot path.** An unattended run resolving from a snapshot is capped at MINIMAL, which is below PARTIAL, so it degrades to research-only immediately whatever the snapshot's age. The **maximum-offline-window knob is scoped to interactive runs only**, and R-15's wording is corrected to match | R1-M4, resolved in favour of §6.5 as the single owner per D-33. Three sections described the same gating differently, and one of them would have had an implementer build a seven-day unattended snapshot path that the degrade trigger forbids. Plan §6.5, §6.6, §10.3, R-15 |
| D-51 (W5 fixer) | RESOLVED | **Each configured language carries a minimum evidence-and-volume band, evaluated at every run**, default deliberately loose, breaching it raising a named digest line and escalating on repetition — never blocking a run and never relaxing a threshold to manufacture volume. **The Czech curation ritual stays merged with the committed weekly Reddit ritual** (W2.5-2) rather than becoming a second obligation | Conductor direction T-3 on R3-B1, option (a). Theme-readiness asserts a *non-empty* candidate set at theme-load time, which a language can satisfy forever while producing one weak candidate a week — valid by the system, thin in reality, and invisible because nothing ever fails. Plan §2.7, §12.1, §10.2 |
| D-52 (W5 fixer) | RESOLVED | **Special-category (Article 9) content is excluded twice: a source- and community-level deny-list applied before collection, and a deterministic lexical check over stored excerpts after it. Both fail to "do not store / delete", never to "flag"** | R4-M1. The binary veto list is a brand-risk filter — a community defined by a health condition or a political affiliation is not "high-severity controversy" — and legitimate interest alone cannot lawfully cover an Article 9 datum. Flagging a record that should not exist is not a control. Plan §2.6, §2.7 |
| D-53 (W5 fixer) | RESOLVED | **The "polish" model role is deleted.** Two text-model roles exist: **drafting** and **judge**. The voice gate is judge-plus-regenerate, and the regenerate call reuses the drafting role | R2-M20. The role was named in §1.5's knob list and in §14.7's version pinning while owning no stage anywhere in the pipeline — a dangling reference that would have produced a configured model nobody calls. Plan §1.5, §10.4, §14.7 |
| D-54 (W5 fixer) | RESOLVED | **Four named regeneration counters, plus one outer bound.** The claim-retry budget (per pack), the voice-regenerate cap (per artifact) **and a new per-pack voice-regenerate allowance**, the spin-regenerate allowance (per artifact), and a new **QA-rejection cap** (per asset slot per pack, default two) with a terminal state — ship the last version labelled "did not clear QA after N attempts". All are subordinate to the **combined per-artifact repair ceiling** (D-41), with check order and exhaustion outcomes stated | R1-M2, R2-M19, R5-F18, R5-F19. A per-artifact voice cap across ~60 assets is a multiplier, not a bound — the claim gate had already diagnosed and fixed exactly this and the voice gate did not inherit the fix; and the QA/human-rejection loop was the one gate in the document with no ladder and no ending at all. Plan §4.9, §14.0, §14.2, §10.4 |
| D-55 (W5 fixer) | RESOLVED | **The single-writer rule was suspended once, deliberately and append-only:** supersession banners were appended in place to `docs/research/SYNTHESIS.md` §3.1's per-language channel table and §7 item 8, pointing at W2.5-4 and D-27. Nothing else in SYNTHESIS was edited. `ARCHITECTURE_PLAN.md` §0.3 also carries the warning for readers who reach the evidence base by another route | Conductor direction T-4 on R1-M12. §0.2 voided the table "for this plan", but SYNTHESIS is not this plan and §0.3 declares it the evidence base while D-21 makes its §4 normative — so an implementer sent to the evidence base would have read the design the operator overruled |
| D-56 (W5 fixer) | RESOLVED | **Phase 0 delivers four calibration artefacts, not two — Czech *and English* structural calibration, Czech *and English* judge golden sets — plus a per-language frozen eval set**, and the do-not-start-Phase-1 gate covers all of them. **A pre-rollout eval comparison (pass rate, human-agreement rate, token cost) is a precondition on any prompt-library or rubric change**, with a blind A/B and a standing spot-check as named deferred-value knobs. Until a language has a golden set, its judge runs deliberately lenient and its flag-rate ceiling is recorded as inactive | R2-M15 and R2-M16, refining D-34 rather than replacing it. The English voice gate would otherwise have run from Phase 2 through Phase 6 — including the trial packs and the first unattended runs — against an uncalibrated judge with no predicted baseline, and the weekly loop was an unmeasured edit path into the system's most safety-relevant prompts. Plan §14.2, §14.8, §12.4, §17 Phase 0 |
| D-57 (W5 fixer) | RESOLVED | **Generated slide backgrounds are produced at 9:16 and the 4:5 carousel slide is derived downward by re-composition** into a template whose content area sits inside a crop-free 4:5 region | R2-M3. Layered re-composition works for overlays, which we composite, and not for the generated raster underneath: a 4:5 background cannot fill 9:16 without cropping (banned), crop-upscaling, padding or outpainting (deferred). The Czech workhorse recipe now serves three vertical destinations, and its vertical slide art was unfunded. Plan §3.2, §4.5, Appendix A.6 |
| D-58 (W5 fixer) | RESOLVED | **The stage-3 script is the artefact of record for every recipe, including carousel-to-reel; slide copy is a rendering of it.** Narration is a reading of the same claim-gated script the captions come from | R2-M4. The source brief's "narration from the slide copy" would have created two candidate artefacts of record in one pipeline, which §6.8 rule 1 ("spoken content is generated only from claim-checked script text") cannot be enforced against. Plan §4.5, §4.2, §6.8 |
| D-59 (W5 fixer) | RESOLVED | **Each licensed vendor's own terms are retrieved, dated and read on one question — does this permit pipeline and derivative use — before the first credit is spent**, with the answer recorded in the vendor roster. **Reddit Pro's terms are read the same way**, because it is a different product from the ordinary-browser-session reading C7 assessed. §2.4's claim that the vendors sell "under terms that permit pipeline use" is converted from a premise into an obligation to demonstrate | R4-M5 and R4-M8. Phase 0 read the router's terms and no vendor's. The timing also moved: the legal reading was scheduled "before any subscription renews beyond the trial", i.e. after data had been ingested and money spent. Plan §2.4, §2.3, §17 Phase 0 |

### Wave 5 — new open decisions (appended 2026-08-06, T25 fixer)

| ID | Status | Decision needed | Gate | Recommendation |
|----|--------|-----------------|------|----------------|
| OD-17a (W5 fixer) | OPEN — **split from OD-17** | Legality posture of a derived-analytics vendor with an **undisclosed** upstream (the short-form trend vendor) | Operator + counsel, **before the first credit is spent** | Treat as the permitted class as a working assumption, with one paragraph of actual legal reading; raw scraped passthrough stays excluded regardless. Independently, read the vendor's own terms for a pipeline-use permission first |
| OD-17b (W5 fixer) | OPEN — **split from OD-17** | Legality posture of the demand-data vendor, whose upstream is **known, published and actively litigated** by the platform against vendors of that class | Operator + counsel, **before the first credit is spent** | Adopt, but **as an explicitly recorded vendor-risk decision** — it was previously taken as an ungated default. This is not "we don't know where they get it" but "we know, and we know the source is suing". The manual CSV export remains the designed degraded state |
| OD-24 (W5 fixer) | OPEN | **Scope of the AI Act's human-editorial-review carve-out** for text, and whether it could ever extend to a synthetic-presenter asset | Qualified counsel, before Phase 6 | Label rather than rely, as the standing interim rule — over-disclosure is not sanctioned, under-disclosure is. §3.3's wording is already downgraded to a working assessment, and the approver-identity field exists so the carve-out is evidenceable if relied on. The deep-fake limb is moot under D-36 and reopens with it |
| OD-25 (W5 fixer) | OPEN | **Primary-text confirmation of the two Czech statutes** the plan relies on (advertising regulation; consumer protection), both carried at low-to-medium confidence with direct retrieval blocked | Counsel or the operator against the statute register, before Phase 6 | Confirm. The controls stand on their own merits either way, but a plan that cites a statute should have read it, and a mis-cited statute discredits the controls that were right. Binds the new recognisability sub-class |
| OD-26 (W5 fixer) | OPEN | **Verification of the EDPB web-scraping guideline reference** the legitimate-interest assessment rests on; and the wholly unassessed **processor / controller / third-country-transfer** area, on which the evidence pack is silent | Operator + counsel, **before the LIA is finalised in Phase 0** — a Phase-0 prerequisite | Verify against the EDPB's own register before finalising, and write the recipient map first, since the privacy notice's Article 13 recipient and transfer sections cannot be drafted without it. A full data-protection review is the honest longer answer |
| OD-27 (W5 fixer) | OPEN | **The employee likeness and voice agreement** — needed only if D-36's ban is lifted | Operator, if and when avatars or voice cloning enter scope | Keep the ban for v1. Explicitly rejected: lifting the ban with a consent-record field designed but no agreement signed — a field pointing at nothing looks like a control and is not one |
| OD-28 (W5 fixer) | OPEN | **Text-model provider and per-role selection** — which model fills the drafting role and which the judge role, per language. A decision the plan could not previously state because the role did not exist (D-39) | Operator at Stage 5 for the shortlist; measured in Phase 2 against the frozen eval set, before the first real packs | Different lineages for drafting and judging (shared blind spots are the failure the separation prevents), and per-language selection — the English drafting choice does not transfer to Czech. Judge weighted on structured-output reliability, drafting on prose and language quality |

---

## Wave 6 — operator amendments after the Stage-5 presentation (appended 2026-08-06)

*The operator issued a new directive while reviewing the Stage-5 presentation, before approval. It overrules one W2.5 operator decision. Logged here; full plan reconciliation is assigned to the implementation xmasterplan (the affected sections are enumerated so nothing reconciles silently).*

| ID | Status | Decision | Rationale / where it lands |
|----|--------|----------|----------------------------|
| W6-1 (operator, 2026-08-06) | RESOLVED — **OVERRULES W2.5-2; amends D-51, W2-01, W4-12** | **No manual pipeline inputs of any kind. The operator supplies exactly two things: configuration, and review-pack decisions.** The weekly curated-inbox ritual (Reddit session and the merged Czech community session) is abolished. Reddit signal is re-sourced on a dual track: **(1) primary — the official Reddit Data API** under a registered app with the commercial-use approval filed as a Phase-0 application (operating volume sits far inside the free rate tier; the blocker B1 found was the approval requirement, not price or capacity); **(2) fallback — a search-index route via the already-licensed demand-data vendor (DataForSEO SERP scoped to reddit.com results)** if approval stalls or is refused, its output classed as low-confidence evidence and never rendered as measured virality (the same replacement rule §2.5 already applies to human-asserted items); **(3) if both are unavailable, Reddit is simply absent** and the per-language volume band (D-51) is the guard. **Scraping remains banned in all cases** — Reddit stays first on the do-not-scrape list; no mirrors, no formerly-open JSON endpoints; F-9 is untouched | Operator directive: "entirely automated pipeline — no manual inputs except config." Consequences accepted and recorded: the curated-inbox source type becomes **empty and dormant** (structure retained, nothing feeds it by default); the W2-01 staleness banner and its abandoned-escalation retire with the ritual; D-51's remedial-ritual clause lapses, so the per-language evidence-and-volume band becomes the **sole** guard against Czech thinness, with Czech coverage now resting on Virlo CZ, Google News CZ, YouTube CZ and DataForSEO CZ. The review-pack human gate is **not** an input and stands unchanged (assignment-mandated). Affected plan sections for the implementation pass: §2.3, §2.5, §8.12, §12.1 (digest lines), §17 Phase 0/1; risk rows W6-01…W6-03 |

| ID | Status | Decision needed | Gate | Recommendation |
|----|--------|-----------------|------|----------------|
| OD-29 (W6) | OPEN | **Outcome of the Reddit Data API commercial-use application** — approved, refused, or unanswered past a deadline the operator sets | Phase 0; the fallback route needs no gate (vendor already licensed) | File the application in week 1 alongside the Meta Ad Library verification; run the DataForSEO fallback from day one regardless, so the pipeline never waits on Reddit's answer; promote the API to primary the day approval lands. Read the Data API developer terms on the pipeline-and-derivative-use question the same way D-59 reads every vendor's |

---

## Wave 7 — operator rulings resolving R10 findings (appended 2026-08-06)

*R10 found that W6-1's own affected-section list never named the curated inbox despite the term appearing 23 times in `ARCHITECTURE_PLAN.md` (F-6); that Phase 7's feedback loop routed outcome capture through a mechanism W6-1 had just abolished (F-6b); and that §3.5's review-budget target and its own per-asset rates disagree by roughly an order of magnitude (F-8). Append-only per the log's own rule: the W6-1 row above is left untouched; W7-1 below corrects its affected-section list rather than rewriting it in place.*

| ID | Status | Decision | Rationale / where it lands |
|----|--------|----------|----------------------------|
| W7-1 (operator, 2026-08-06) | RESOLVED — **corrects W6-1's affected-section list; amends D-25, D-51, OD-12, OD-16, W2-08a/D-28, W6-03** | **All six curated-inbox surfaces are reclassified as CONFIGURATION INPUTS, never pipeline inputs**: LinkedIn Ad Library (biweekly human sweep; no API exists), Google Trends CSV export (the designed degraded state for the demand axis), TikTok Creative Center (monthly browse; also the terminal rung of the trend-vendor fallback ladder per OD-16 option (c)), LinkedIn organic (P2, human only, permanently — scraping would be the operator's own contractual breach), Czech-native venues (human-only), and the long-tail P2 group (GitHub trending, podcasts, review sites, Discord/Slack). The operator may still browse any of the six whenever they choose; what they find becomes configuration — watch topics, surface forms, excludes, competitor names, vendor-roster entries — never a per-run data drop a run consumes or waits on. No run ever blocks on a human; no source in this class carries a staleness flag that escalates to an abandoned tier; no pack is ever labelled "pain axis: operator-fed". This is consistent with W6-1's own wording ("the operator supplies configuration and review decisions only") — the ruling here is that W6-1 already implied this and was never reconciled against the plan text | R10 F-6: `W6-1` appears zero times in `ARCHITECTURE_PLAN.md` while "curated inbox" appears 23 times, so the reconciliation W6-1 promised never happened. **W6-1's affected-section list is corrected here to the complete list**: §2.2, §2.3, §8.12, §9.1, §10.2, §12.1, §13.3, §15 R-06, §16.2 OD-16, §17 Phase 0/1/7, §18. **Consequences recorded honestly**: the ad-creative axis is Meta-only in v1 (the LinkedIn Ad Library half becomes config-time competitor naming, not a second collected axis) — amends D-25's licensed-portfolio framing where it touches ad-creative; the demand axis loses its CSV degraded state and rests entirely on DataForSEO, closing OD-12's "manual Trends CSV retained as the designed degraded state" clause; **the trend axis loses its terminal fallback rung** — if Virlo fails and Shortimize fails there is no rung below them, only absence, amending OD-16's three-rung description; the Czech signal loses two of its four §2.3-counted carriers (LinkedIn organic, Czech-native venues), amending W6-03's residual-risk acceptance (superseded at RISK_LOG.md W6-03a) and W2-08a's destination reasoning where it assumed a LinkedIn organic carrier. RISK_LOG.md W2-01 is superseded in full at row W2-01a; plan §15 R-06 needs the same retirement, flagged for the plan's owning writer |
| W7-2 (operator, 2026-08-06) | RESOLVED — **adds a third permitted operator-input class alongside W6-1's two (configuration; review-pack decisions)** | **Post-hoc outcome notes are a third permitted operator input.** Phase 7's feedback loop (assignment step 10) designed outcome capture "through the same curated-inbox mechanism" — a mechanism W7-1 has just abolished as a pipeline input, and one the system could never have fed from itself since it never publishes and so cannot observe engagement. The operator may attach a dated note to something already published; the note is an annotation about a completed fact, not a per-run data drop | These notes are read only by processes that operate *between* runs — the monthly calibration report (R-21/OD-20), R-23's lagging Czech short-form indicator — and never by a run's own collection, ranking or generation stages, which is exactly the property W6-1 exists to protect: "the operator supplies configuration and review decisions only" stays true by adding a class that cannot steer a run, not by bending either existing class. Affected: §12.1 (Phase 7 feedback loop redesigned around the note as a calibration input, not a collection input), §17 Phase 7, W6-1's two-input statement (now three) |
| W7-3 (operator, 2026-08-06) | RESOLVED — **operator adopts all four corrective levers (a)–(d) together; supersedes OD-8's topics-per-run recommendation downward** | Closes §3.5's review-burden arithmetic gap (target: five topics reviewed in under thirty minutes; actual, by the section's own per-asset rates: 3–5.5 hours per pack run). **(a) Keyframe-first approval** — the digest approval step approves still keyframes; video renders asynchronously afterward and returns for a materially shorter confirm-only pass. **(b) Default destination matrix cut** — X stays off (already default-off per D-30/OD-21, restated here as a review-load lever, not only a publish-gate one); one of TikTok/Reels/Shorts video per language per run rather than all three destination-derivatives reviewed independently; blog off by default (narrows OD-14's drafts-only scope to config-gated opt-in). **(c) Default topics per run cut from five to three**, revising OD-8's recommendation and D-38/D-29's masters-per-language cap math downward proportionally. **(d) "Approved unread" is a distinct, separately logged decision class** in the review-decision store — batch approval without individual consideration — and **may not be used as evidence for the AI Act editorial-review carve-out (OD-24)** | R10 F-8. The four levers are adopted together because none alone closes the gap: (a) removes the single largest per-asset review cost without removing the human from the loop; (b) and (c) cut the volume under review; (d) does not reduce load at all but stops the system from silently manufacturing compliance evidence that is not there once load wins anyway. Affected: §3.5 (target restated against the new default volume), §3.2/§10.4 (destination-matrix and topic-count defaults), §7.2/§7.6 (keyframe-first approval step), §12.1/§12.5 (approval-decision-store schema gains the "approved unread" class), §16 OD-24 (exclusion stated as a rule, not a caveat), §16 OD-8 (recommendation superseded downward); new risk logged at RISK_LOG.md W7-02 |

---

## Amendment A — safety amendment merged before Stage-5 approval (appended 2026-08-06)

*Append-only. Amendment A is the pre-approval half of the playbook-layer programme (`docs/plans/PLAYBOOK_LAYER/00_MASTERPLAN.md` §5). It was authored as three leaf documents — `docs/architecture/playbook/A1_prohibited_outcome_gate.md`, `A2_depiction.md`, `A3_emptiability.md` — and merged into `ARCHITECTURE_PLAN.md` under single-writer discipline. Identifiers follow the namespace ruling C-5: decisions `PB-D-n`, risks `PB-R-n`, legal opens keep their own `OD-L` prefix and are **not** renumbered into the `OD-8…OD-29` sequence. The behaviour differential is at plan Appendix B.6.*

| ID | Status | Decision | Rationale / where it lands |
|----|--------|----------|----------------------------|
| PB-D-1 (2026-08-06) | RESOLVED — **satisfies STAGE5_APPROVAL_SUMMARY.md §5's approval precondition** | **Amendment A is applied.** It closes three defects that apply to every tenant and depend on nothing in the playbook layer: **P-10** (the claim ledger could launder an unlawful claim) via the Prohibited-Outcome Gate; **P-11** (generated imagery made unchecked claims about real products) via check class 12 and reference grounding; **P-1** (a tenant with no product could never run, and its theme could never be scheduled) via legitimate emptiability of F-B/F-C/F-E under an external-verifier substitution floor | The approval summary's §5 previously read "approval is conditional on Amendment A landing first." That condition is now met. Amendment A deliberately touches none of: relation types, archetypes, angles, genres, CTA vocabulary, trigger lanes, node overlays, ranking profiles, or the config surface — all of which remain Amendment B. Lands at plan §0.3, §1.5, §3.5, §4.2a, §4.4, §4.9, §5.4a, §5.6, §6.3, §6.4, §6.5, §6.7, §6.9, §7.7, §8.11, §8.12, §10.3, §10.4, §11.3, §11.4, §12.1, §12.2, §12.4, §13.2, §13.4, §14 intro, §14.0, §14.1a (new), §14.3, §14.6, §16.2, §17, Appendix B.1/B.4/B.6 |
| PB-D-2 (2026-08-06) | RESOLVED | **The Prohibited-Outcome Gate's drop terminal is registered as a named exception inside §14.0's exhaustion rule, not as a repair allowance outside the combined ceiling.** §14.0's rule states that an artifact exhausting the combined per-artifact repair ceiling takes the downgrade-repair variant if one exists, and otherwise enters the pack labelled. **Both branches are unsafe for this gate**: it has no downgrade-repair variant by design (a softened version of the same assertion is the same unlawful claim with its evidence trail deleted), and an asset packed with a "did not clear the Prohibited-Outcome Gate" label would ship the literal prohibited assertion into the review package | The exception is placed at the narrowest point actually broken — the exhaustion *outcome*, not the exhaustion *counting*. The Gate's regenerate still draws on the existing per-pack claim-retry budget and creates **no fifth counter**, so §4.9's "four distinct regeneration budgets" stays true. A parallel budget was rejected because it would have recreated exactly the unstated cross-gate interaction §14.0 exists to prevent. Lands at §14.0, §14.1a, §4.9 |
| PB-D-3 (2026-08-06) | RESOLVED — **applies masterplan ruling C-12; may not be re-decided by Amendment B Wave 0** | **Check class 12 is deterministic-only in v1.** It verifies that every asset flagged as depicting a real sellable item carries a linked, in-window reference in its provenance record — a pure provenance check with **no machine-vision model call**, adding no node and no new per-call ceiling. A model-mediated comparison node that takes two images and judges their resemblance is explicitly **not** this check; it is registered as deferred value with its cost named rather than promised and unbudgeted | The substantive visual comparison — is this item depicted as it is actually sold — is the **human attestation's** job, and the review pack must show the side-by-side rather than link to it. The engine contributes only the gate: a provenance record and a coherent reference exist, which prevents orphaned images with no grounding claim. See PB-R-4 for the residual risk this leaves. Lands at §6.7, §4.2a, §4.4, §14.3, §14.6, §12.2 |
| PB-D-4 (2026-08-06) | RESOLVED — **flagged for restatement at Amendment B Wave 0** | **The bounded model-node inventory goes from thirteen to fourteen.** The Gate's semantic paraphrase pass is a real model call and is registered as **N-14**, distinct from N-10 (the claim substrate's semantic pass) because their output vocabularies and gate-ordering constraints differ structurally. It inherits a per-call ceiling from §5.4a's existing per-node-class rule and is named as a term in the cost decomposition | **This collides with masterplan ruling C-1, which states "inventory stays at thirteen for text nodes."** C-1 was ruling on a different question — annex `01`'s incorrect claim that node N-8 was *added* when it already existed — and was not addressed to a genuinely new model call. Leaving the Gate's semantic pass unbudgeted would reproduce precisely the defect C-12 was raised to close: a safety control costed at nothing. **C-1's count must be restated when Amendment B Wave 0 runs**; it is recorded here rather than silently absorbed. Lands at §1.5, §5.4a, §14.1a, Appendix B.6 |
| PB-D-5 (2026-08-06) | RESOLVED — **grants a capability; operator-visible** | **A theme may opt into depiction Policy B or C directly, today, without any playbook mechanism existing.** Annex `02` §3.3 reserved Policy-B selection to playbooks (food & beverage, product/e-commerce). No playbook layer exists yet, so the choice was between locking every theme to Policy A until Amendment B ships, or giving a theme a direct, precondition-gated opt-in. The second was taken | Amendment A must stand alone with zero playbooks defined, and the operator has confirmed real product promotion is on the critical path — a design that closes P-11 by making product depiction impossible until Amendment B would fail the operator's actual requirement. The opt-in mirrors the existing **hero-tier promotion** shape (engine-safe default with an explicit, bounded, human-gated escalation), not a new control class. Preconditions: at least one F-B item with a populated, tenant-attested F-W reference; the human-attestation mechanism available; an explicit, dated declaration. F-W being non-empty is **never** read as consent to depict. A theme may always tighten back to Policy A unilaterally, and may never declare "B without the grounding requirement." The opt-in reads forward unchanged when Amendment B's playbooks begin selecting B for a vertical — no migration. Lands at §10.4, §4.2a, §7.7 |
| PB-D-6 (2026-08-06) | RESOLVED — **applies masterplan ruling C-4; may not be re-decided by Amendment B Wave 0** | **F-B, F-C and F-E become legitimately emptiable under a three-part test, and substitution is required rather than exemption.** F-B at whole-catalogue level; F-C **only jointly with F-B empty**; F-E at whole-set level following F-B. Where any is empty, its descriptive content is replaced by a **grounded substitute requiring an external verifier** — a site check (a visible page element matching the claim's specificity, not an HTTP status), a resolving URL (a page that renders claim-supporting content, not a link checker), or a third-party record (an independent source carrying audit authority, not a self-submitted directory listing). Operator attestation suffices **only** for the bare fact of absence; the moment a substitute says anything more it is descriptive-or-stronger and needs an external verifier. **An F-B substitute must carry a negative-capability field**, or the substitution is rejected and check class 6 degrades to blocking all descriptive statements for that tenant | F-C's joint rule exists because independent emptiability would let a tenant hold live offers with no negative-capability grounding, silently removing check class 6's basis. **F-F takes a deliberately different fix**: it is a *rule*, not offer data, and is never made emptiable — the engine supplies a safe default ("prices are never stated") whenever the price-value classes are themselves empty or unresolved, so a tenant with nothing to price never authors a pricing rule by hand. A legitimately-empty catalogue lands at **PARTIAL**, never FULL (a substitute is categorically thinner than direct corroboration) and never INSUFFICIENT (which is the defect). Lands at §6.3 notes 1–2, §6.4, §6.5, §6.7, §6.9, §10.3, §13.2 |
| PB-D-7 (2026-08-06) | RESOLVED — **corrects the masterplan's own A4 edit list** | **§13.2 (theme-readiness validation) is added to Amendment A's merge scope.** The masterplan's §5 A4 row lists the sections to edit and omits §13.2 — yet the same masterplan's P-1 defect row names §13.2 as P-1's second, independently-failing bite point ("readiness fails → may never be scheduled") | Fixing only §6.5's Step-1 gate would have closed the per-run half of P-1 and left the admission-gate half open, so an offer-less theme would still have been permanently barred from the schedule. Readiness now names *which* of the three tests is unmet rather than reporting an undifferentiated "unresolved", **never fails a theme for having zero offers in itself**, and never names F-F as a failure reason. Lands at §13.2 |
| PB-D-8 (2026-08-06) | RESOLVED — **closes an independently-logged review finding in passing** | **§6.5's "one of the four fail-closed triggers enumerated at §11.3" is corrected to "five."** §11.3 has enumerated five triggers since the fifth ("a gate that cannot execute") was added; §6.5 was never updated | This is R10 finding **F-54**, triaged in that review to "before Phase 1." Amendment A edits this exact sentence family, and leaving a known-wrong count in text being revised would be negligent rather than disciplined. The trigger **count stays at five** — the Gate's own unavailability is registered as an application of the existing fifth trigger, not a new sixth class. Lands at §6.5, §11.3 |
| PB-D-9 (2026-08-06) | RESOLVED — **supersedes a wire-in row in annex `02`** | **The Prohibited-Outcome Gate's placement in the gate-order diagram lands at §14's introduction, not §6.10.** Annex `02_legal_claim_packs.md` §7's wire-in table directed the diagram edit to §6.10 | §6.10 explicitly disclaims ownership of the per-asset chain, and plan §0.2's single-owner rule states that "the canonical per-asset gate chain lives in §14." A chain drawn in two places becomes two different chains, which this architecture has already paid for once. The Gate's own design — contents, two states, mode-invariance, the three authorisation closures, the enforcement ladder, the escalating digest event — is given a home at the **new §14.1a**, which A1's own wire-in table failed to allocate. Lands at §14 intro, §14.1a |
| W8-1 (operator, 2026-08-06) | RESOLVED — **closes the pending decision recorded at RISK_LOG.md PB-R-1** | **Theme #1 will opt into depiction Policy B.** The operator wants the product-depiction capability back (AI-generated imagery of the product's own dashboard/interface), accepting the grounding regime that comes with it | The decision here is the *intent*; the formal opt-in itself happens at configuration time in Phase 0/1 and still requires every precondition at §10.4: current, tenant-attested screenshots of the actual interface supplied as F-W reference imagery linked to the relevant F-B offer entry; an explicit, dated opt-in declaration; and the per-asset human side-by-side attestation before any publish decision. Until those exist, theme #1 sits at Policy A — the readiness check will refuse an opt-in with zero attested references (PB-D-5). Lands at §10.4 config work in Phase 0/1; PB-R-1 is resolved as a decision and open as a task |
| W8-2 (operator, 2026-08-06) | RESOLVED — **affirms the acceptance RISK_LOG.md W6-03a reopened, in exactly its terms** | **The Czech-carrier reduction is accepted as actually stated**: Virlo CZ, Google News CZ, YouTube CZ and DataForSEO CZ are the whole of the Czech signal axis — two of the four §2.3-counted carriers (LinkedIn organic, Czech-native venues) are removed from the carrier count itself, not merely from a remedy path | W6-03a ruled the prior acceptance (W6-03) was granted against a smaller loss and must be treated as pending "until the operator affirms it in exactly these terms." This row is that affirmation. The config-level remedy menu stands unchanged (enable additional CZ sources, widen Virlo CZ views, raise DataForSEO CZ keyword coverage); if Czech topic quality proves thin in practice, the response is config widening, not a design reopen. Lands at RISK_LOG.md W6-03a (affirmed), §2.3, §2.7 |
| PB-OD-L1…L5, L8 (2026-08-06) | OPEN — **registered, not resolved** | **Six legal open items are carried into the plan under their own `OD-L` prefix and deliberately not renumbered** into the `OD-8…OD-29` sequence, per C-5. OD-L1 (Reg. 1924/2006 Art. 10 and the Arts. 13/14 register mechanism) · OD-L2 (Dir. 2001/83/EC Title VIII and the "medicinal product by presentation" doctrine) · OD-L3 (MDR Art. 7 and undeclared medical-device classification risk) · OD-L4 (Reg. 1169/2011 Art. 7(3)) · OD-L5 (UCPD Annex I's cure-claim item) · OD-L8 (whether a licensed-claims pathway should exist in a later version) | **None of these citations was verified against primary text**; direct EUR-Lex retrieval was attempted for two and returned blocked or empty responses. They are graded Medium to Medium-High and recorded at that grading in §0.3, unpromoted. The controls they justify are warranted on their reasoning regardless of how the article numbers land — over-blocking is the Gate's design intent — so **no relaxation is permitted pending confirmation**. OD-L1–L5 join the existing Phase-6 counsel gate alongside OD-24/25/26. OD-L8's interim posture is: do not build; a genuinely licensed tenant authors regulated-claim content entirely outside this pipeline, by hand. See PB-R-5. Lands at §0.3, §13.4, §16.2, §17 |
