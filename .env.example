# Copy to .env and fill in real values. .env is gitignored and must NEVER be committed.
# Precedence in the engine: real environment variable > .env > legacy secrets/*.key (deprecated).
VIRLO_API_KEY=
POSTIZ_API_KEY=
KIE_API_KEY=
OPENROUTER_API_KEY=
HANDLE_HASH_KEY=
